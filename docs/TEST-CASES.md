# Test Cases

Generated by JBS IntelliQE — 21 test case(s).

| Module | Cases |
|---|---|
| Authentication | 21 |

## Authentication

### TC-001 — Verify successful login with valid credentials redirects to Dashboard and creates a session
**Feature:** User Login · **Priority:** P0 · **Type:** positive · **Severity:** Critical

Confirms the primary authorized gateway works: a registered user with valid credentials authenticates and lands on the Dashboard with an active session. This is the highest-risk path because any failure blocks all authorized access.

**Precondition:** The OrangeHRM QA login page is reachable at https://opensource-demo.orangehrmlive.com/web/index.php/auth/login. A registered user account exists with username 'Admin' and password 'admin123'. No active session exists in the browser.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: Login page loads over HTTPS displaying the OrangeHRM logo, Username field, Password field, Login button, and 'Forgot your password?' link
2. Enter 'Admin' into the field labelled 'Username' → Expected: The Username field shows 'Admin' and no validation message is displayed
3. Enter 'admin123' into the field labelled 'Password' → Expected: The Password field shows masked characters and no validation message is displayed
4. Click the 'Login' button → Expected: The user is authenticated and redirected to the Dashboard at /web/index.php/dashboard/index within 3 seconds, with the top navigation and Dashboard widgets visible

**Expected Result:** The registered user is authenticated, redirected to the Dashboard, and a valid authenticated session is established.

**Tags:** smoke, regression, auth

### TC-002 — Verify login fails with 'Invalid credentials' when password is wrong
**Feature:** User Login · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Confirms the system rejects a valid username paired with an incorrect password, surfaces the exact error message, and keeps the user on the login page. Mitigates the risk of unauthorized access via credential guessing.

**Precondition:** The OrangeHRM QA login page is reachable. A registered user account exists with username 'Admin' and password 'admin123'. No active session exists.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: Login page is displayed with Username and Password fields and the Login button
2. Enter 'Admin' into the Username field → Expected: The Username field shows 'Admin'
3. Enter 'wrongPass999' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error banner is displayed, the URL remains on /auth/login, and no session is created

**Expected Result:** Authentication is rejected, the 'Invalid credentials' error is shown, and the user stays on the login page.

**Tags:** regression, auth

### TC-003 — Verify login fails with 'Invalid credentials' when username does not exist
**Feature:** User Login · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms an unknown username paired with any password is rejected with the generic error, ensuring the system does not distinguish between bad username and bad password (avoids user enumeration).

**Precondition:** The OrangeHRM QA login page is reachable. No account exists for username 'ghostUser01'. No active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter 'ghostUser01' into the Username field → Expected: The Username field shows 'ghostUser01'
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error is displayed and the user remains on the login page with no session created

**Expected Result:** Authentication is rejected with the same generic 'Invalid credentials' message, preventing username enumeration.

**Tags:** regression, auth, security

### TC-004 — Verify 'Required' validation appears when Username is blank and Login is clicked
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms the mandatory Username rule fires and blocks authentication when the field is empty. Prevents silent submission of incomplete forms.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed with both fields empty
2. Leave the Username field blank and enter 'admin123' into the Password field → Expected: The Password field shows masked characters; the Username field remains empty
3. Click the 'Login' button → Expected: A 'Required' validation message is displayed directly beneath the Username field, no request to authenticate is made, and the user stays on the login page

**Expected Result:** A field-level 'Required' message is shown for Username and authentication does not proceed.

**Tags:** regression, auth, data-validation

### TC-005 — Verify 'Required' validation appears when Password is blank and Login is clicked
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms the mandatory Password rule fires and blocks authentication when the field is empty, mirroring the Username rule for the second credential input.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed with both fields empty
2. Enter 'Admin' into the Username field and leave the Password field blank → Expected: The Username field shows 'Admin'; the Password field remains empty
3. Click the 'Login' button → Expected: A 'Required' validation message is displayed directly beneath the Password field and authentication does not proceed

**Expected Result:** A field-level 'Required' message is shown for Password and authentication does not proceed.

**Tags:** regression, auth, data-validation

### TC-006 — Verify 'Required' validation appears for both fields when the form is submitted empty
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms both mandatory-field messages render simultaneously when the user submits with nothing entered, ensuring validation is per-field and not short-circuited after the first error.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed with both fields empty
2. Without entering any values, click the 'Login' button → Expected: A 'Required' validation message is displayed beneath the Username field AND beneath the Password field, and the user remains on the login page with no session created

**Expected Result:** Both fields show 'Required' validation messages and authentication does not proceed.

**Tags:** regression, auth, data-validation

### TC-007 — Verify Password field masks characters as they are typed
**Feature:** Password Masking · **Priority:** P2 · **Type:** security · **Severity:** Major

Confirms the password is never rendered in plain text on screen, protecting credential confidentiality from shoulder-surfing. Directly validates the 'passwords must never be displayed in plain text' security requirement.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Type 'Secr3t!Pass' into the Password field → Expected: Each typed character is rendered as a masked dot/bullet rather than the literal character
3. Inspect the Password input element's type attribute via browser DevTools → Expected: The input element has type='password', confirming the value is not exposed as plain text in the DOM rendering

**Expected Result:** The password value is displayed only as masked characters and never as readable plain text.

**Tags:** regression, auth, security, ui

### TC-008 — Verify pressing Enter with valid credentials submits the login form
**Feature:** Keyboard Submission · **Priority:** P2 · **Type:** positive · **Severity:** Moderate

Confirms the Enter key triggers authentication identically to clicking Login, supporting keyboard-driven submission. Mitigates the low but real risk of the convenience path being broken.

**Precondition:** The OrangeHRM QA login page is reachable. A registered account exists with username 'Admin' and password 'admin123'. No active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter 'Admin' into the Username field → Expected: The Username field shows 'Admin'
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. With focus in the Password field, press the Enter key → Expected: The login process is triggered and the user is redirected to the Dashboard, equivalent to clicking the Login button

**Expected Result:** Pressing Enter authenticates the user and redirects to the Dashboard.

**Tags:** regression, auth

### TC-009 — Verify pressing Enter with empty fields triggers 'Required' validation instead of submitting
**Feature:** Keyboard Submission · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Confirms keyboard submission is subject to the same mandatory-field validation as the button, so an empty Enter press does not bypass field checks.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed with both fields empty
2. Click into the Username field to give it focus, leaving both fields empty → Expected: The Username field has focus and remains empty
3. Press the Enter key → Expected: A 'Required' validation message is displayed for the empty mandatory field(s) and the form is not submitted; the user remains on the login page

**Expected Result:** Enter with empty fields shows 'Required' validation and does not authenticate.

**Tags:** regression, auth, data-validation

### TC-010 — Verify 'Forgot your password?' link navigates to the Password Reset page
**Feature:** Forgot Password · **Priority:** P2 · **Type:** positive · **Severity:** Moderate

Confirms the account-recovery entry point routes the user to the reset page. Mitigates the risk of users being locked out with no recovery path.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed with a visible 'Forgot your password?' link
2. Click the 'Forgot your password?' link → Expected: The browser navigates to the Password Reset page at /web/index.php/auth/requestPasswordResetCode showing the 'Reset Password' heading and a Username field with Cancel and Reset Password buttons

**Expected Result:** The user is redirected to the Password Reset page to initiate recovery.

**Tags:** regression, auth, ui

### TC-011 — Verify Logout returns the user to the Login page and terminates the session
**Feature:** Logout · **Priority:** P1 · **Type:** positive · **Severity:** Major

Confirms an authenticated user can end their session and is returned to the login page. Mitigates the risk of an open session being reused on a shared machine.

**Precondition:** A registered user is logged in as 'Admin' and is currently on the Dashboard with an active session.

**Steps:**

1. From the Dashboard, click the user dropdown menu in the top-right header → Expected: A dropdown menu opens showing 'About', 'Support', 'Change Password', and 'Logout' options
2. Click the 'Logout' option → Expected: The session is ended and the browser is redirected to the login page at /web/index.php/auth/login

**Expected Result:** The user session is terminated and the login page is displayed.

**Tags:** regression, auth

### TC-012 — Verify browser Back button after Logout does not restore the cached Dashboard
**Feature:** Logout · **Priority:** P1 · **Type:** security · **Severity:** Major

Confirms the session is invalidated on logout so the cached authenticated Dashboard cannot be restored via the Back button. Mitigates session-reuse risk on shared devices.

**Precondition:** A registered user has just logged out and is on the login page. The Dashboard was previously visited in this browser tab's history.

**Steps:**

1. Confirm the current page is the login page immediately after logout → Expected: The login form is displayed
2. Click the browser Back button → Expected: The authenticated Dashboard is NOT restored from cache; the user is kept on or redirected back to the login page
3. Attempt to interact with any control that would require an active session → Expected: No protected Dashboard content is accessible; the user must re-authenticate

**Expected Result:** The Back button does not expose the cached Dashboard; the invalidated session forces re-authentication.

**Tags:** regression, auth, security

### TC-013 — Verify direct navigation to a protected Dashboard URL without a session redirects to Login
**Feature:** User Login · **Priority:** P1 · **Type:** security · **Severity:** Critical

Confirms unauthorized access to protected pages is blocked when no active session exists, covering the AC9 unauthorized-access path. Mitigates the risk of bypassing login via a bookmarked URL.

**Precondition:** No user is logged in and no active session cookie exists in the browser.

**Steps:**

1. Ensure no session exists by clearing cookies or opening a fresh private window → Expected: No authenticated session is present
2. Navigate directly to https://opensource-demo.orangehrmlive.com/web/index.php/dashboard/index → Expected: The application redirects to the login page at /web/index.php/auth/login and does not render any Dashboard content

**Expected Result:** Access to the protected Dashboard without a session is denied and the user is redirected to Login.

**Tags:** regression, auth, security

### TC-014 — Verify leading and trailing spaces in Username are trimmed before authentication
**Feature:** Field Validation · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Confirms the Username trim rule allows a valid user to authenticate even when the entry has surrounding whitespace, validating the data rule that Username leading/trailing spaces are trimmed.

**Precondition:** The OrangeHRM QA login page is reachable. A registered account exists with username 'Admin' and password 'admin123'. No active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter '  Admin  ' (with two leading and two trailing spaces) into the Username field → Expected: The Username field accepts the value including surrounding spaces
3. Enter 'admin123' into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: The surrounding spaces are trimmed, the credentials are treated as 'Admin' / 'admin123', and the user is authenticated and redirected to the Dashboard

**Expected Result:** Username whitespace is trimmed and authentication succeeds.

**Tags:** regression, auth, data-validation

### TC-015 — Verify Password is treated as exact-match with spaces NOT trimmed
**Feature:** Field Validation · **Priority:** P2 · **Type:** data · **Severity:** Major

Confirms passwords are not normalized: a stored password without surrounding spaces must be rejected when the entry adds spaces, contrasting with the Username trim rule. Mitigates the risk of weakening credential exactness.

**Precondition:** The OrangeHRM QA login page is reachable. A registered account exists with username 'Admin' and exact password 'admin123' (no surrounding spaces). No active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter 'Admin' into the Username field → Expected: The Username field shows 'Admin'
3. Enter ' admin123 ' (with one leading and one trailing space) into the Password field → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: The password is NOT trimmed, so it does not match the stored value; an 'Invalid credentials' error is displayed and the user remains on the login page

**Expected Result:** The space-padded password is treated as an exact non-matching value and login is rejected.

**Tags:** regression, auth, data-validation

### TC-016 — Verify Username accepts values at the 1-character and 100-character length boundaries
**Feature:** Field Validation · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Confirms the Username field enforces its stated min (1) and max (100) length boundaries by accepting entries exactly at each limit. Validates the data rule on Username length.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists. Test accounts may not exist for these values; the check focuses on field acceptance and validation behavior, not successful authentication.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter the single character 'a' into the Username field, enter 'admin123' into the Password field, and click 'Login' → Expected: The 1-character Username is accepted by the field (no 'Required' or length error); the form submits and returns an 'Invalid credentials' error since the account is unknown
3. Clear the fields, enter a 100-character string of 'a' into the Username field, enter 'admin123' into the Password field, and click 'Login' → Expected: The 100-character Username is accepted without truncation error; the form submits and returns 'Invalid credentials'

**Expected Result:** The Username field accepts values at both the 1-character and 100-character boundaries without length-validation errors.

**Tags:** regression, auth, data-validation

### TC-017 — Verify Username input containing an HTML/script payload is sanitized and not executed
**Feature:** Field Validation · **Priority:** P1 · **Type:** security · **Severity:** Critical

Confirms script/HTML injected into the Username field is neutralized and never executed as active content, validating the XSS-prevention security requirement for the login surface.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter the string <script>alert('xss')</script> into the Username field → Expected: The literal characters are shown as text in the field with no dialog triggered on entry
3. Enter 'admin123' into the Password field and click 'Login' → Expected: No JavaScript alert dialog is executed; the payload is treated as an ordinary (invalid) username and an 'Invalid credentials' error is returned
4. Confirm the page did not reflect the payload as executable markup by inspecting the rendered response → Expected: The payload is HTML-encoded/sanitized in any reflected output and does not run as script

**Expected Result:** The injected script is sanitized, never executes, and login is rejected as invalid credentials.

**Tags:** regression, auth, security

### TC-018 — Verify SQL injection payload in login fields does not bypass authentication
**Feature:** User Login · **Priority:** P1 · **Type:** security · **Severity:** Critical

Confirms a classic SQL injection string in the credential fields is treated as literal data and cannot authenticate or corrupt the query, validating the SQL-injection-prevention security requirement.

**Precondition:** The OrangeHRM QA login page is reachable and no active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter admin' OR '1'='1 into the Username field → Expected: The value is accepted as literal text
3. Enter anything' OR '1'='1 into the Password field and click 'Login' → Expected: Authentication is NOT bypassed; an 'Invalid credentials' error is displayed and the user is not redirected to the Dashboard

**Expected Result:** The SQL injection attempt fails, returning 'Invalid credentials' with no unauthorized access.

**Tags:** regression, auth, security

### TC-019 — Verify repeated failed login attempts are throttled to prevent brute-force
**Feature:** User Login · **Priority:** P2 · **Type:** security · **Severity:** Major

Confirms the application resists automated credential guessing by throttling, delaying, or blocking after repeated failures, validating the brute-force-prevention security requirement.

**Precondition:** The OrangeHRM QA login page is reachable. A valid account 'Admin' exists but will be targeted with wrong passwords. No active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Submit the Login form with username 'Admin' and an incorrect password repeatedly for at least 10 consecutive attempts → Expected: Each attempt returns 'Invalid credentials'
3. Observe the response behavior across the successive failed attempts → Expected: The application applies a brute-force countermeasure such as a CAPTCHA challenge, an enforced delay, or a temporary lockout, rather than accepting unlimited rapid attempts

**Expected Result:** Repeated failed attempts trigger a throttling/lockout countermeasure that impedes brute-force guessing.

**Tags:** regression, auth, security

### TC-020 — Verify credentials are transmitted over HTTPS and the password never appears in URL, history, or logs
**Feature:** User Login · **Priority:** P1 · **Type:** security · **Severity:** Critical

Confirms the password is sent only over an encrypted channel and is never exposed in the URL/query string, browser history, or client-visible logs, validating the HTTPS-transport and no-plaintext-logging security requirements.

**Precondition:** The OrangeHRM QA login page is reachable. A valid account 'Admin' / 'admin123' exists. Browser DevTools Network tab is available for inspection.

**Steps:**

1. Open browser DevTools, switch to the Network tab, and navigate to the login page → Expected: The page and its resources load over HTTPS (scheme https://) with a valid certificate
2. Enter 'Admin' and 'admin123', click 'Login', and inspect the authentication request in the Network tab → Expected: The login request is sent over HTTPS using the POST method with credentials in the request body, not in the URL/query string
3. Inspect the resulting URL and the browser history entry for the request → Expected: Neither the URL nor the browser history contains the password value in plain text
4. Review any client-accessible console output or response payload → Expected: The password value does not appear in logs, console output, or the response body

**Expected Result:** The password is transmitted only over HTTPS in the request body and is absent from the URL, browser history, and logs.

**Tags:** regression, auth, security

### TC-021 — Verify Username case-sensitivity behavior matches the configured authentication policy
**Feature:** User Login · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms the login handles Username casing per the configured policy, so that a case variant of a valid username behaves consistently. Mitigates the risk of ambiguous authentication outcomes for casing.

**Precondition:** The OrangeHRM QA login page is reachable. A registered account exists with the canonical username 'Admin' and password 'admin123'. The system authentication policy treats the OrangeHRM username as case-sensitive. No active session exists.

**Steps:**

1. Navigate to the login page → Expected: Login form is displayed
2. Enter 'admin' (all lowercase) into the Username field and 'admin123' into the Password field → Expected: The fields accept the entered values
3. Click the 'Login' button → Expected: The outcome matches the configured case-sensitivity policy: for the case-sensitive OrangeHRM policy, 'admin' does not match 'Admin' and an 'Invalid credentials' error is returned rather than a successful login

**Expected Result:** Username casing is handled per the configured policy, producing a consistent and documented authentication result.

**Tags:** regression, auth, data-validation
