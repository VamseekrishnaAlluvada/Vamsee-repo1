# Test Cases

Generated by JBS IntelliQE — 170 test case(s).

| Module | Cases |
|---|---|
| API | 170 |

## API

### TC-169 — Verify GET /posts returns 200 with a non-empty JSON array of posts
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the list endpoint responds successfully, returns JSON content, a non-empty array body, and stays within the response-time budget.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke, list

### TC-170 — Confirm GET /posts returns list items each exposing a numeric id field
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates that the first element of the returned collection carries the resource identifier as a number, matching the posts resource contract.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.id" is present. → Expected: The JSON field "0.id" is present in the response body.
4. Assert that JSON "0.id" is of type number. → Expected: The JSON field "0.id" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "0.id" is present; JSON "0.id" is of type number.

**Tags:** api, schema, list

### TC-171 — Return 404 for GET /posts/99999999 when the post id does not exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests a single post by an id far outside the seeded range and asserts the API reports the resource as not found rather than returning data or a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, not-found

### TC-172 — Reject GET /posts/abc with a 4xx when a non-numeric id is supplied
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a non-numeric value where the numeric id path parameter is expected and asserts the API responds with a client error rather than a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-173 — Verify GET /posts/1 returns 200 with a non-empty JSON post object
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented read request succeeds and returns a JSON body carrying the requested post.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-174 — Confirm GET /posts/1 returns id as a number matching the requested path id 1
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the identifier field is present, numeric, and equals the id supplied in the path.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "id" equals 1. → Expected: The JSON field "id" is present and equals 1.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "id" is of type number; JSON "id" equals 1.

**Tags:** api, schema, posts

### TC-175 — Return 404 for GET /posts/99999999 when the post id does not exist
**Feature:** Posts API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

A read against a well-formed but non-existent id must report the resource as not found.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, posts

### TC-176 — Reject GET /posts/not-a-number with a 4xx when the id is non-numeric
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The id segment expects a number; a malformed path value must not resolve to a valid post or a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation, posts

### TC-177 — Reject DELETE-less write via POST /posts/1 with a 4xx as the read path rejects unsupported methods
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

POST is not defined on the single-post read URL; the endpoint must respond with a documented client error, not a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method, posts

### TC-178 — Verify GET /comments?postId=1 returns 200 with a non-empty JSON array of comments
**Feature:** Comments API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path request succeeds, responds with JSON, and carries at least one comment for post 1.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke

### TC-179 — Confirm GET /comments?postId=1 returns each element carrying a numeric id and string email
**Feature:** Comments API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates that list items expose the documented comment fields with the correct primitive types.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.id" is of type number. → Expected: The JSON field "0.id" is present and is of type number.
4. Assert that JSON "0.postId" equals 1. → Expected: The JSON field "0.postId" is present and equals 1.
5. Assert that JSON "0.email" is of type string. → Expected: The JSON field "0.email" is present and is of type string.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "0.id" is of type number; JSON "0.postId" equals 1; JSON "0.email" is of type string.

**Tags:** api, schema

### TC-180 — Return an empty array with 200 for GET /comments?postId=99999999 when no comments match
**Feature:** Comments API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Verifies that filtering by a postId that cannot exist yields a successful, empty result set rather than an error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, filter

### TC-181 — Ensure GET /comments?postId=abc with a non-numeric filter does not fault with 5xx
**Feature:** Comments API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Confirms a malformed query value is handled gracefully — the service returns a client-side status, never a 500.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative

### TC-182 — Confirm GET /comments?postId=1&unknownParam=foo ignores an unknown query parameter and still returns 200
**Feature:** Comments API · **Priority:** P3 · **Type:** negative · **Severity:** Minor

Verifies that an unrecognised query parameter is safely ignored and the request still succeeds with a JSON array.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=1&unknownParam=foo is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=1&unknownParam=foo. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.

**Expected Result:** All 2 assertions pass — response status is 200; JSON body is a non-empty array.

**Tags:** api, negative

### TC-183 — Verify POST /posts with the documented body returns 200 and a JSON content-type
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented create payload and confirms the endpoint responds successfully with a JSON body and acceptable latency.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, create

### TC-184 — Confirm POST /posts returns a body echoing the submitted userId as 1 and a generated id
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Checks that a successful create returns the persisted resource, including the submitted userId and a server-assigned id.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
4. Assert that JSON "userId" equals 1. → Expected: The JSON field "userId" is present and equals 1.
5. Assert that JSON "title" is of type string. → Expected: The JSON field "title" is present and is of type string.

**Expected Result:** All 4 assertions pass — response is successful (2xx); JSON "id" is present; JSON "userId" equals 1; JSON "title" is of type string.

**Tags:** api, schema, create

### TC-185 — Reject POST /posts request that omits the required title field without a 500 server error
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Drops the title field from the payload and asserts the API handles the missing required field with a client-level response rather than crashing.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"body":"Created from Postman collection","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative, validation

### TC-186 — Reject POST /posts with a 4xx when the request body is syntactically malformed JSON
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a broken JSON body to confirm the endpoint fails cleanly with a client error instead of a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"title":"QA Automation Post","body":"broken","userId":1] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative, malformed

### TC-187 — Reject DELETE against POST-only collection URL /posts with a non-2xx status
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Issues an unsupported HTTP method on the collection endpoint and asserts the server does not treat it as a successful create.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, method

### TC-188 — Verify PUT /posts/1 with the full documented body returns 200 and a JSON content-type
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy-path update: submits the documented id/title/body/userId payload and confirms the endpoint responds 200 with a JSON body echoing the resource id.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-189 — Confirm PUT /posts/1 returns id as a number and title as a string in the updated resource
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Schema guard on the update response: verifies the echoed id is numeric and the title is a string, matching the shape of the submitted payload.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "title" is of type string. → Expected: The JSON field "title" is present and is of type string.
5. Assert that JSON "id" equals 1. → Expected: The JSON field "id" is present and equals 1.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "id" is of type number; JSON "title" is of type string; JSON "id" equals 1.

**Tags:** api, schema, posts

### TC-190 — Reject PUT /posts/1 with a 4xx when the body omits the required title field
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative validation: sends a payload missing the title field and asserts the endpoint does not treat it as a successful, complete update.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"id":1,"body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative, validation, posts

### TC-191 — Reject PUT /posts/1 with a 4xx when the request body is malformed JSON
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative validation: submits a syntactically broken JSON body and asserts the server responds with a client error rather than a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title","body":] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative, malformed, posts

### TC-192 — Return 404 for PUT /posts/99999999 targeting a post id that does not exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative case: attempts to update a non-existent resource id and asserts the endpoint reports the resource as not found rather than silently succeeding.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/99999999 carrying the JSON request body shown above. [data: {"id":99999999,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, not-found, posts

### TC-193 — Reject PUT /posts/not-a-number with a 4xx when the path id is non-numeric
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Malformed-path negative: uses a non-numeric id where a numeric identifier is required and asserts the server returns a client error, not a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/not-a-number carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, malformed-path, posts

### TC-194 — Verify PATCH /posts/1 returns 200 with a JSON body when updating the title field
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented PATCH request updating the title and asserts the exact success status, JSON content-type and a non-empty body.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"title":"Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-195 — Confirm PATCH /posts/1 echoes id as a number and the patched title as a string
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the response schema after the partial update: the resource id remains a number and the updated title is returned as a string carrying the submitted value.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"title":"Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "id" equals 1. → Expected: The JSON field "id" is present and equals 1.
5. Assert that JSON "title" is of type string. → Expected: The JSON field "title" is present and is of type string.
6. Assert that JSON "title" equals "Patched Title". → Expected: The JSON field "title" is present and equals "Patched Title".

**Expected Result:** All 5 assertions pass — response status is 200; JSON "id" is of type number; JSON "id" equals 1; JSON "title" is of type string; JSON "title" equals "Patched Title".

**Tags:** api, schema, posts

### TC-196 — Reject PATCH /posts/1 with a 4xx when the request body is malformed JSON
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON body to confirm the endpoint refuses it with a client error rather than a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"title": "Patched Title"] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, payload, posts

### TC-197 — Verify PATCH /posts/99999999 for a non-existent post does not return a 5xx error
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Attempts to patch an id that cannot exist and asserts the API responds with a clean client-side outcome (typically 404) rather than crashing with a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/99999999 carrying the JSON request body shown above. [data: {"title":"Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative, posts

### TC-198 — Reject PATCH /posts/abc with a 4xx when a non-numeric id is supplied in the path
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Uses a non-numeric value where the numeric post id is required and asserts the endpoint returns a client error instead of a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/abc carrying the JSON request body shown above. [data: {"title":"Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, path, posts

### TC-199 — Verify DELETE /posts/1 returns 200 confirming the post is deleted
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented delete request for an existing post id and asserts the exact success status and JSON content-type.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, delete

### TC-200 — Confirm DELETE /posts/1 responds within 5000ms on the happy path
**Feature:** Posts API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards the delete latency for an existing post to catch performance regressions.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response is successful (2xx); response time is under 5000 ms.

**Tags:** api, performance, delete

### TC-201 — Confirm DELETE /posts/99999999 does not return a 5xx for a non-existent post id
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Deletes an id that cannot exist and asserts the service handles it cleanly rather than crashing.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative, delete

### TC-202 — Reject DELETE /posts/not-a-number with a 4xx for a non-numeric id path
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Supplies a non-numeric id where a numeric id is required and asserts a client error rather than success.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-203 — Return a 4xx for an unsupported HTTP TRACE method against /posts/1
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unsupported method to the delete URL to confirm the endpoint rejects it with a client error instead of a server crash.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP TRACE request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-204 — Verify GET /posts/9999 returns 200 with a non-empty JSON post object
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented request succeeds, responds with a JSON content-type, and carries a post body.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/9999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/9999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-205 — Confirm GET /posts/9999 returns id as a number on the happy path
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the type of the top-level id field so schema regressions are caught for the read operation.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/9999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/9999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
4. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "id" is present; JSON "id" is of type number.

**Tags:** api, schema, posts

### TC-206 — Return 404 for GET /posts/99999999 when the post id cannot exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests an out-of-range numeric id on the posts resource and expects a not-found response rather than a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, posts

### TC-207 — Reject GET /posts/not-a-number with a 4xx when a non-numeric id is supplied
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends a malformed, non-numeric id where the posts resource expects a numeric id and expects a client error, never a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation, posts

### TC-208 — Verify GET /ping returns 200 confirming the service health-check endpoint is up
**Feature:** Ping API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented no-parameter request to the ping health-check and asserts the exact expected status code and that a response body is returned within a healthy time budget.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text". → Expected: The "content-type" response header is present and contains "text".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text".

**Tags:** api, smoke, health

### TC-209 — Confirm GET /ping responds under 5000ms on the happy path
**Feature:** Ping API · **Priority:** P1 · **Type:** edge · **Severity:** Moderate

Guards the ping health-check performance so a slow but 200 response is still flagged; asserts a 2xx status with a response-time ceiling.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response is successful (2xx); response time is under 5000 ms.

**Tags:** api, performance, health

### TC-210 — Confirm GET /ping ignores an unknown query parameter and still returns 200 without a 500
**Feature:** Ping API · **Priority:** P2 · **Type:** negative · **Severity:** Minor

Appends an unrecognised query parameter to the health-check to prove it is safely ignored rather than causing a server error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response status is 200; response status is under 500.

**Tags:** api, robustness

### TC-211 — Reject POST /ping against the read-only health-check with a 4xx/405 error
**Feature:** Ping API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends an unsupported HTTP method to the same ping URL to confirm the endpoint does not accept writes and returns a client error rather than a 2xx.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/ping. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 404, 405.

**Tags:** api, method-validation

### TC-212 — Return 404 for GET /ping/99999999 when an unknown sub-path is requested
**Feature:** Ping API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Requests a non-existent resource under the ping path to confirm the API returns a not-found error and does not fall through to a 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is 404.

**Tags:** api, negative

### TC-213 — Verify POST /auth with valid admin credentials returns 200 and issues a token
**Feature:** Auth API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented login request succeeds, responds with JSON, and returns a non-empty token property within an acceptable time.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin","password":"password123"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "token" is present. → Expected: The JSON field "token" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "token" is present; response time is under 5000 ms.

**Tags:** api, smoke, auth

### TC-214 — Confirm POST /auth returns the issued token as a string in the 200 response body
**Feature:** Auth API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the response schema of a successful login: the token field is present and typed as a string.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin","password":"password123"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "token" is present. → Expected: The JSON field "token" is present in the response body.
4. Assert that JSON "token" is of type string. → Expected: The JSON field "token" is present and is of type string.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "token" is present; JSON "token" is of type string.

**Tags:** api, schema, auth

### TC-215 — Reject POST /auth with a 4xx when the required password field is missing
**Feature:** Auth API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a login payload containing only the username and asserts the endpoint does not issue a token for an incomplete credential set.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, auth

### TC-216 — Return 400 for POST /auth when the request body is malformed JSON
**Feature:** Auth API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Submits a syntactically broken JSON payload and asserts the endpoint rejects it with a client error rather than a server crash.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin","password":}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 415, 422. → Expected: The response status code is one of 400, 415, 422.

**Expected Result:** All 1 assertion pass — response status is one of 400, 415, 422.

**Tags:** api, negative, malformed

### TC-217 — Reject POST /auth with a 4xx when submitting invalid credentials
**Feature:** Auth API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a well-formed body carrying wrong credentials and asserts the endpoint does not return a valid session token.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"wronguser","password":"wrongpass"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that JSON "token" is present. → Expected: The JSON field "token" is present in the response body.

**Expected Result:** All 2 assertions pass — response status is under 500; JSON "token" is present.

**Tags:** api, negative, auth

### TC-218 — Verify POST /booking creates a booking and returns 200 with a JSON body
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Submits the fully documented booking payload and confirms the endpoint accepts it, responds with exactly 200, and returns a JSON content-type.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname":"{{firstname}}","lastname":"Tester","totalprice":{{totalprice}},"depositpaid":true,"bookingdates":{"checkin":"2026-10-01","checkout":"2026-10-05"},"additionalneeds":"Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-219 — Confirm POST /booking returns a bookingid and a booking object echoing the submitted data
**Feature:** Booking API · **Priority:** P0 · **Type:** data · **Severity:** Critical

Verifies the create response is a non-empty JSON object exposing a top-level bookingid and a nested booking object that reflects the request payload.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname":"{{firstname}}","lastname":"Tester","totalprice":{{totalprice}},"depositpaid":true,"bookingdates":{"checkin":"2026-10-01","checkout":"2026-10-05"},"additionalneeds":"Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "bookingid" is present. → Expected: The JSON field "bookingid" is present in the response body.
4. Assert that JSON "bookingid" is of type number. → Expected: The JSON field "bookingid" is present and is of type number.
5. Assert that JSON "booking" is of type object. → Expected: The JSON field "booking" is present and is of type object.
6. Assert that JSON "booking.lastname" equals "Tester". → Expected: The JSON field "booking.lastname" is present and equals "Tester".

**Expected Result:** All 5 assertions pass — response status is 200; JSON "bookingid" is present; JSON "bookingid" is of type number; JSON "booking" is of type object; JSON "booking.lastname" equals "Tester".

**Tags:** api, schema, booking

### TC-220 — Reject POST /booking with a non-2xx status when the required firstname field is missing
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a booking payload that omits the required firstname field and asserts the endpoint does not accept it as a successful creation.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"lastname":"Tester","totalprice":{{totalprice}},"depositpaid":true,"bookingdates":{"checkin":"2026-10-01","checkout":"2026-10-05"},"additionalneeds":"Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-221 — Reject POST /booking with a non-2xx status when the request body is malformed JSON
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Posts a syntactically broken JSON payload and confirms the endpoint fails the request instead of creating a booking or returning a 5xx crash-style success.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname":"{{firstname}}","lastname":"Tester","totalprice":,}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, malformed

### TC-222 — Reject POST /booking with a non-2xx status when totalprice is a string instead of a number
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Submits a wrong-typed totalprice value to confirm the endpoint validates field types and does not silently accept an invalid booking.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname":"{{firstname}}","lastname":"Tester","totalprice":"not-a-number","depositpaid":true,"bookingdates":{"checkin":"2026-10-01","checkout":"2026-10-05"},"additionalneeds":"Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-223 — Reject POST /booking with a non-2xx status when an empty JSON object is sent
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Posts an empty object with no booking fields and asserts the endpoint does not treat it as a valid creation request.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-224 — Verify GET /booking/ returns 200 with a non-empty JSON array of bookings
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the list endpoint responds successfully, returns application/json, and the body is a non-empty array of booking summaries.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array.

**Tags:** api, smoke, booking

### TC-225 — Confirm GET /booking/ returns each list item with a numeric bookingid field
**Feature:** Booking API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the response schema for the list endpoint by asserting the first element exposes a bookingid property of type number.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.bookingid" is present. → Expected: The JSON field "0.bookingid" is present in the response body.
4. Assert that JSON "0.bookingid" is of type number. → Expected: The JSON field "0.bookingid" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "0.bookingid" is present; JSON "0.bookingid" is of type number.

**Tags:** api, schema, booking

### TC-226 — Verify GET /booking/ responds within 5000ms on the happy path
**Feature:** Booking API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards the list endpoint response time to catch performance regressions on the documented request.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, booking

### TC-227 — Return 404 for GET /booking/99999999 when the booking id does not exist
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests a single booking via the sibling GET /booking/{id} operation with an id that cannot exist and expects a 404 Not Found.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, booking

### TC-228 — Confirm GET /booking/ ignores an unknown query parameter and still returns 200
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unsupported query parameter and asserts the list endpoint tolerates it by returning a 200 array rather than failing with a 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/?bogusparam=abc123 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/?bogusparam=abc123. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.

**Expected Result:** All 2 assertions pass — response status is 200; JSON body is a non-empty array.

**Tags:** api, negative, booking

### TC-229 — Verify GET /booking/ filtered by firstname returns 200 with a JSON array
**Feature:** Booking API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Exercises the documented firstname filter on the list endpoint and asserts a 200 response whose body is a JSON array (possibly empty when no match).

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/?firstname=John is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/?firstname=John. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, filter, booking

### TC-230 — Verify GET /booking?lastname=Tester returns 200 with a JSON array of bookings
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the booking list endpoint responds successfully to the documented filtered request and returns a JSON array body.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=Tester is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=Tester. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "root" is of type array.

**Tags:** api, smoke, booking

### TC-231 — Confirm GET /booking?lastname=Tester responds within 5000ms on the happy path
**Feature:** Booking API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards the response-time SLA for the documented filtered booking list request.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=Tester is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=Tester. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, booking

### TC-232 — Verify GET /booking with a non-matching lastname filter returns 200 and an empty-capable JSON array
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A list endpoint filtered by a lastname that matches no records should return 200 with a JSON array (possibly empty), not a 404.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=NoSuchPersonXYZ99999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=NoSuchPersonXYZ99999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "root" is of type array.

**Tags:** api, filtering, booking

### TC-233 — Confirm GET /booking ignores an unknown query parameter and still returns 200 with a JSON array
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An unrecognised query parameter alongside the endpoint should be safely ignored rather than causing a 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=Tester&bogusParam=abc123 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=Tester&bogusParam=abc123. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 2 assertions pass — response status is under 500; JSON "root" is of type array.

**Tags:** api, robustness, booking

### TC-234 — Reject PUT against the /booking list URL with a non-2xx status (405 or 404)
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The collection URL only supports GET (list) and POST (create); a PUT to the base collection must not succeed.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=Tester is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking?lastname=Tester. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, method, booking

### TC-235 — Verify PUT /booking/ with a complete valid booking payload returns 200 with a JSON response body
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented booking update payload and confirms the endpoint responds successfully with a non-empty JSON body within an acceptable response time.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-236 — Return a 4xx for PUT /booking/99999999 when updating a booking id that does not exist
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Targets a booking id that cannot exist to confirm the endpoint rejects updates to unknown resources rather than silently succeeding or erroring with 5xx.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/99999999 carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, not-found

### TC-237 — Reject PUT /booking/abc with a 4xx when a non-numeric booking id is supplied in the path
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Uses a non-numeric booking id where a numeric identifier is required to confirm the endpoint returns a client error rather than a server error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/abc carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, validation

### TC-238 — Reject PUT /booking/ with a 4xx when the required firstname field is missing from the payload
**Feature:** Booking API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Drops the required firstname field from the booking payload to confirm the endpoint validates the request and refuses an incomplete update.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, validation

### TC-239 — Return 400 for PUT /booking/ when the request body is malformed JSON
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON body to confirm the endpoint rejects the request with a client error instead of a 5xx crash.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester",] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, malformed

### TC-240 — Reject PUT /booking/ with a 4xx when totalprice is sent as a string instead of a number
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Supplies a string value for the numeric totalprice field to confirm the endpoint enforces field types and returns a client error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":"five hundred","depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, validation

### TC-241 — Verify PATCH /booking/ with a valid totalprice returns 200 and a JSON body
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented partial-update request succeeds and responds with a non-empty JSON payload of the correct content type.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"totalprice": 999}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-242 — Reject PATCH /booking/ with a 4xx when the request body is malformed JSON
**Feature:** Booking API · **Priority:** P0 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON body and asserts the API refuses it rather than returning a success or a 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"totalprice": 999] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, booking

### TC-243 — Reject PATCH /booking/ with a 4xx when the request body is empty and carries no fields
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Submits an empty JSON object so the required totalprice field is absent and asserts the update is not accepted.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation, booking

### TC-244 — Return 404 for PATCH /booking/99999999 when the target booking id cannot exist
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Targets a non-existent booking id under the booking resource and asserts the API reports the resource as not found.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/99999999 carrying the JSON request body shown above. [data: {"totalprice": 999}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 404, 405.

**Tags:** api, negative, booking

### TC-245 — Reject PATCH /booking/not-a-number with a 4xx when a non-numeric id is supplied
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Uses an alphabetic value where a numeric booking id is expected and asserts the API rejects it cleanly without a server error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/not-a-number carrying the JSON request body shown above. [data: {"totalprice": 999}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation, booking

### TC-246 — Verify DELETE /booking/ with a valid token cookie returns 200 confirming the booking is removed
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented DELETE request with a valid token credential and asserts the endpoint responds with the expected success status within the response-time budget.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-247 — Return 401/403 for DELETE /booking/ when no token credential is supplied
**Feature:** Booking API · **Priority:** P0 · **Type:** security · **Severity:** Critical

Confirms the delete operation is protected: without the token cookie the endpoint must refuse the request rather than delete anything.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 401, 403. → Expected: The response status code is one of 401, 403.

**Expected Result:** All 1 assertion pass — response status is one of 401, 403.

**Tags:** api, auth, negative

### TC-248 — Reject DELETE /booking/ with a malformed bogus token cookie by returning 401/403
**Feature:** Booking API · **Priority:** P1 · **Type:** security · **Severity:** Major

Supplies a well-formed but invalid token cookie and asserts the endpoint rejects it instead of performing the deletion.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 401, 403. → Expected: The response status code is one of 401, 403.

**Expected Result:** All 1 assertion pass — response status is one of 401, 403.

**Tags:** api, auth, negative

### TC-249 — Return 404/405 for DELETE /booking/99999999 when the booking id does not exist
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Targets a booking id that cannot exist and asserts the endpoint responds with a client error rather than a success or a 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 404, 405.

**Tags:** api, negative, booking

### TC-250 — Reject DELETE /booking/not-a-number with a 4xx when a non-numeric booking id is supplied
**Feature:** Booking API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Sends a non-numeric path segment where a numeric booking id is required and asserts the endpoint returns a client error and stays below 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-251 — Verify POST /auth/login with valid credentials returns 200 and a JSON body
**Feature:** Login API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Submits the documented username/password/expiresInMins payload and confirms the login endpoint authenticates successfully with a JSON response.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","password":"emilyspass","expiresInMins":30}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "username" is present. → Expected: The JSON field "username" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "username" is present; response time is under 5000 ms.

**Tags:** api, smoke, auth

### TC-252 — Confirm POST /auth/login echoes the authenticated username as 'emilys' on success
**Feature:** Login API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the successful login response reflects the submitted username, confirming the correct account was authenticated.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","password":"emilyspass","expiresInMins":30}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "username" equals "emilys". → Expected: The JSON field "username" is present and equals "emilys".
4. Assert that JSON "username" is of type string. → Expected: The JSON field "username" is present and is of type string.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "username" equals "emilys"; JSON "username" is of type string.

**Tags:** api, schema

### TC-253 — Reject POST /auth/login with a 4xx when the password field is missing
**Feature:** Login API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Sends a login payload containing only the username and confirms the endpoint rejects the incomplete request rather than authenticating.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","expiresInMins":30}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 401, 422. → Expected: The response status code is one of 400, 401, 422.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 401, 422.

**Tags:** api, negative, validation

### TC-254 — Reject POST /auth/login with 400 when the request body is malformed JSON
**Feature:** Login API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON body and confirms the endpoint returns a client error instead of a 500.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","password":}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, payload

### TC-255 — Reject POST /auth/login with a 4xx when the password is incorrect
**Feature:** Login API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Submits a valid username with a wrong password and confirms the endpoint denies authentication.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","password":"wrongpassword","expiresInMins":30}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 401. → Expected: The response status code is one of 400, 401.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 401.

**Tags:** api, negative, auth

### TC-256 — Reject POST /auth/login with a 4xx when an empty JSON object is submitted
**Feature:** Login API · **Priority:** P1 · **Type:** edge · **Severity:** Major

Sends an empty object body with no credentials and confirms the endpoint returns a client error rather than authenticating or crashing.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 401, 422. → Expected: The response status code is one of 400, 401, 422.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 401, 422.

**Tags:** api, negative, payload

### TC-257 — Verify GET /auth/me returns 200 with a non-empty JSON body
**Feature:** Auth Me API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented request to the me resource responds successfully, serves JSON and returns a populated body within an acceptable time.

**Precondition:** The API endpoint https://dummyjson.com/auth/me is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/me. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke

### TC-258 — Confirm GET /auth/me returns the current user with id typed as a number
**Feature:** Auth Me API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the top-level id field of the me object is present and of numeric type, which is the stable identifier for the resource.

**Precondition:** The API endpoint https://dummyjson.com/auth/me is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/me. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
4. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "id" is present; JSON "id" is of type number.

**Tags:** api, schema

### TC-259 — Confirm GET /auth/me ignores an unknown query parameter and still returns 200
**Feature:** Auth Me API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unrecognised query parameter to ensure the endpoint tolerates it gracefully rather than failing with a 5xx error.

**Precondition:** The API endpoint https://dummyjson.com/auth/me?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/me?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is under 500; header "content-type" contains "application/json".

**Tags:** api, robustness

### TC-260 — Reject POST /auth/me with a non-2xx status because only GET is supported
**Feature:** Auth Me API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Issues an unsupported HTTP method against the me URL and asserts the API refuses it rather than treating it as a read.

**Precondition:** The API endpoint https://dummyjson.com/auth/me is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/me. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, method

### TC-261 — Verify GET /products with limit=10&skip=10&select=title,price returns 200 with a JSON body
**Feature:** Products API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy path: the documented list request responds successfully with a JSON content type and a non-empty body.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=10&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=10&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "products" is present. → Expected: The JSON field "products" is present in the response body.
5. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
6. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 5 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "products" is present; JSON "products" is of type array; response time is under 5000 ms.

**Tags:** api, smoke, products

### TC-262 — Confirm GET /products honours select=title,price by returning title as a string and price as a number on the first item
**Feature:** Products API · **Priority:** P1 · **Type:** data · **Severity:** Major

Schema check: the select projection is reflected in each list item, exposing the requested title and price fields with the correct types.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=10&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=10&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "products.0.title" is of type string. → Expected: The JSON field "products.0.title" is present and is of type string.
4. Assert that JSON "products.0.price" is of type number. → Expected: The JSON field "products.0.price" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "products.0.title" is of type string; JSON "products.0.price" is of type number.

**Tags:** api, schema, products

### TC-263 — Confirm GET /products echoes the requested limit=10 and skip=10 pagination values in the response envelope
**Feature:** Products API · **Priority:** P1 · **Type:** api · **Severity:** Major

Positive pagination check: the response reports back the limit and skip that were requested so clients can trust the pagination window.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=10&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=10&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "limit" equals 10. → Expected: The JSON field "limit" is present and equals 10.
4. Assert that JSON "skip" equals 10. → Expected: The JSON field "skip" is present and equals 10.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "limit" equals 10; JSON "skip" equals 10.

**Tags:** api, pagination, products

### TC-264 — Verify GET /products with a non-numeric limit does not crash the server and stays below 500
**Feature:** Products API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative validation: a malformed limit query value must be handled cleanly rather than triggering a server error.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=abc&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=abc&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, validation, products

### TC-265 — Reject POST /products against the list URL with a non-2xx status since only GET is supported for listing
**Feature:** Products API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Negative method check: issuing a write verb against the read-only list endpoint must not succeed.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=10&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/products?limit=10&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, method, products

### TC-266 — Verify GET /products ignores an unknown query parameter and still returns 200 with a product list
**Feature:** Products API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Resilience check: an unrecognised query parameter should be safely ignored rather than causing an error.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=10&skip=10&select=title,price&bogusParam=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=10&skip=10&select=title,price&bogusParam=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "products" is of type array.

**Tags:** api, resilience, products

### TC-267 — Verify GET /products/search?q=phone returns 200 with a JSON body containing the products list
**Feature:** Products Search API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented search request succeeds, returns JSON, and carries a products collection in the body.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=phone. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "products" is present. → Expected: The JSON field "products" is present in the response body.
5. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
6. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 5 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "products" is present; JSON "products" is of type array; response time is under 5000 ms.

**Tags:** api, smoke, search

### TC-268 — Confirm GET /products/search?q=phone returns products as an array and total as a number
**Feature:** Products Search API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the shape of the search envelope: the products collection is an array and the total count is numeric.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=phone. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
4. Assert that JSON "total" is of type number. → Expected: The JSON field "total" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "products" is of type array; JSON "total" is of type number.

**Tags:** api, schema, search

### TC-269 — Verify GET /products/search with a no-match query returns 200 and an empty products array
**Feature:** Products Search API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

A search term that matches no products should still succeed with 200 and an empty collection rather than an error.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=zzzznomatchqueryzzzz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=zzzznomatchqueryzzzz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
4. Assert that JSON "total" equals 0. → Expected: The JSON field "total" is present and equals 0.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "products" is of type array; JSON "total" equals 0.

**Tags:** api, search, edge

### TC-270 — Confirm GET /products/search ignores an unknown query parameter and still returns 200
**Feature:** Products Search API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An unrecognised query parameter alongside q should be safely ignored, yielding a successful 2xx rather than a server error.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone&bogusParam=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=phone&bogusParam=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response status is under 500. → Expected: The response status code is below 500.
4. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.

**Expected Result:** All 3 assertions pass — response is successful (2xx); response status is under 500; JSON "products" is of type array.

**Tags:** api, search, robustness

### TC-271 — Reject POST /products/search with a non-2xx status because the search endpoint only supports GET
**Feature:** Products Search API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sending a write method to the read-only search endpoint must not succeed; expect a 4xx/405 rather than a 2xx.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/products/search?q=phone. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, search, method

### TC-272 — Verify POST /carts/add returns 200 with a JSON cart body when a valid userId and products array are supplied
**Feature:** Carts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy path: submits the documented request body and confirms the endpoint responds successfully with a JSON payload representing the newly created cart.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1,"products":[{"id":144,"quantity":4},{"id":98,"quantity":1}]}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, carts

### TC-273 — Confirm POST /carts/add echoes userId 1 and returns a products array in the created cart
**Feature:** Carts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Schema check on the create response: verifies the returned cart carries the submitted userId and exposes a products collection of the expected type.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1,"products":[{"id":144,"quantity":4},{"id":98,"quantity":1}]}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "userId" equals 1. → Expected: The JSON field "userId" is present and equals 1.
5. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "id" is of type number; JSON "userId" equals 1; JSON "products" is of type array.

**Tags:** api, schema, carts

### TC-274 — Reject POST /carts/add with a 4xx when the required products field is missing from the body
**Feature:** Carts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative validation: omits the required products array and expects the endpoint to refuse the request rather than create an empty cart.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 404, 422. → Expected: The response status code is one of 400, 404, 422.

**Expected Result:** All 1 assertion pass — response status is one of 400, 404, 422.

**Tags:** api, negative, validation

### TC-275 — Return 400 for POST /carts/add when the request body is syntactically malformed JSON
**Feature:** Carts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative payload: sends a broken JSON string and expects the server to reject it with a client error instead of a 500.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1,"products":[{"id":144,"quantity":4}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.

**Expected Result:** All 1 assertion pass — response status is one of 400, 422.

**Tags:** api, negative, malformed

### TC-276 — Reject POST /carts/add with a 4xx when userId is sent as a string instead of a number
**Feature:** Carts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Negative type validation: submits a wrong-typed userId to confirm the endpoint enforces the numeric contract rather than accepting the mismatched value.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":"one","products":[{"id":144,"quantity":4}]}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.

**Expected Result:** All 1 assertion pass — response status is one of 400, 422.

**Tags:** api, negative, type-check

### TC-277 — Verify GET /v1/forecast returns 200 with a non-empty JSON body for the Mumbai coordinates
**Feature:** Forecast API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented forecast request for latitude 19.07 / longitude 72.87 responds successfully with a JSON content-type and a populated body within an acceptable time budget.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "latitude" is present. → Expected: The JSON field "latitude" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "latitude" is present; response time is under 5000 ms.

**Tags:** api, smoke, forecast

### TC-278 — Confirm GET /v1/forecast echoes numeric latitude/longitude and returns the requested current block
**Feature:** Forecast API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the top-level fields grounded in the request — latitude and longitude as numbers, the timezone string, and the current object requested via the current= query — are present with the correct types.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "latitude" is of type number. → Expected: The JSON field "latitude" is present and is of type number.
4. Assert that JSON "longitude" is of type number. → Expected: The JSON field "longitude" is present and is of type number.
5. Assert that JSON "timezone" is of type string. → Expected: The JSON field "timezone" is present and is of type string.
6. Assert that JSON "current" is of type object. → Expected: The JSON field "current" is present and is of type object.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "latitude" is of type number; JSON "longitude" is of type number; JSON "timezone" is of type string; JSON "current" is of type object.

**Tags:** api, schema, forecast

### TC-279 — Reject GET /v1/forecast with 400 when latitude is a non-numeric value
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends latitude=abc where a decimal number is required and confirms the API returns a client error rather than a 200 or a 500.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, validation

### TC-280 — Reject GET /v1/forecast with 400 when the required latitude parameter is omitted
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Drops the mandatory latitude query parameter and confirms the endpoint responds with a client error instead of defaulting or crashing.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, validation

### TC-281 — Confirm GET /v1/forecast ignores an unknown query parameter and still returns 200
**Feature:** Forecast API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Appends an unsupported parameter (foo=bar) to the documented request and verifies the API safely ignores it, returning a successful JSON response rather than a server error.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata&foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata&foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "latitude" is present. → Expected: The JSON field "latitude" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "latitude" is present.

**Tags:** api, edge, robustness

### TC-282 — Verify GET /v1/forecast returns 200 with a non-empty JSON forecast body for Bangalore coordinates
**Feature:** Forecast API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy path: the documented daily-forecast request for latitude 12.97/longitude 77.59 in Asia/Kolkata responds successfully with a JSON body.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "daily" is present. → Expected: The JSON field "daily" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "daily" is present; response time is under 5000 ms.

**Tags:** api, smoke, forecast

### TC-283 — Confirm GET /v1/forecast echoes latitude/longitude as numbers and returns a daily object in the response
**Feature:** Forecast API · **Priority:** P1 · **Type:** data · **Severity:** Major

Schema check grounded in the request parameters: the response should carry numeric latitude and longitude and an object holding the requested daily fields.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "latitude" is of type number. → Expected: The JSON field "latitude" is present and is of type number.
4. Assert that JSON "longitude" is of type number. → Expected: The JSON field "longitude" is present and is of type number.
5. Assert that JSON "daily" is of type object. → Expected: The JSON field "daily" is present and is of type object.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "latitude" is of type number; JSON "longitude" is of type number; JSON "daily" is of type object.

**Tags:** api, schema, forecast

### TC-284 — Reject GET /v1/forecast with 4xx when latitude is out of the valid -90..90 range
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative validation: an out-of-range latitude (999) is an invalid query value and must be rejected rather than returning a forecast or a 500.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=999&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=999&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, validation

### TC-285 — Reject GET /v1/forecast with 400 when the required latitude parameter is missing
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative validation: dropping the mandatory latitude parameter (longitude only) must yield a client error, not a partial or successful response.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, validation

### TC-286 — Reject GET /v1/forecast with 4xx when latitude is a non-numeric value
**Feature:** Forecast API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Negative validation: a malformed latitude ('abc') where a number is required must be rejected with a client error and must not return a forecast.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, malformed

### TC-287 — Confirm GET /v1/forecast ignores an unknown query parameter and still returns 200
**Feature:** Forecast API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Resilience: adding an unsupported query parameter should be safely ignored, yielding a successful forecast rather than a 500.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata&bogus_param=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata&bogus_param=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "daily" is present. → Expected: The JSON field "daily" is present in the response body.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "daily" is present.

**Tags:** api, edge, resilience

### TC-288 — Verify GET /simple/price for bitcoin,ethereum in usd,inr returns 200 with a JSON body
**Feature:** Simple Price API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented request succeeds, responds as JSON, and carries the requested coin quotes.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "bitcoin" is present. → Expected: The JSON field "bitcoin" is present in the response body.
5. Assert that JSON "ethereum" is present. → Expected: The JSON field "ethereum" is present in the response body.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "bitcoin" is present; JSON "ethereum" is present.

**Tags:** api, smoke, price

### TC-289 — Confirm GET /simple/price returns bitcoin.usd and ethereum.inr as numeric quote values
**Feature:** Simple Price API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the requested currency conversions exist under each coin key and are numbers, matching the ids/vs_currencies query parameters.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "bitcoin.usd" is of type number. → Expected: The JSON field "bitcoin.usd" is present and is of type number.
4. Assert that JSON "bitcoin.inr" is of type number. → Expected: The JSON field "bitcoin.inr" is present and is of type number.
5. Assert that JSON "ethereum.usd" is of type number. → Expected: The JSON field "ethereum.usd" is present and is of type number.
6. Assert that JSON "ethereum.inr" is of type number. → Expected: The JSON field "ethereum.inr" is present and is of type number.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "bitcoin.usd" is of type number; JSON "bitcoin.inr" is of type number; JSON "ethereum.usd" is of type number; JSON "ethereum.inr" is of type number.

**Tags:** api, schema, price

### TC-290 — Verify GET /simple/price happy path responds within 5000 ms
**Feature:** Simple Price API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Performance guard ensuring the documented price lookup returns promptly under normal conditions.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, price

### TC-291 — Reject GET /simple/price with 4xx when the required vs_currencies parameter is missing
**Feature:** Simple Price API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Omits the mandatory vs_currencies query parameter and expects the API to reject the request rather than return a successful body.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.

**Expected Result:** All 1 assertion pass — response status is one of 400, 422.

**Tags:** api, negative, validation

### TC-292 — Reject POST /simple/price with 4xx/405 since only GET is supported on the price endpoint
**Feature:** Simple Price API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unsupported HTTP method to the price URL and expects a client error rather than a successful response.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 404, 405. → Expected: The response status code is one of 400, 404, 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 404, 405.

**Tags:** api, negative, method

### TC-293 — Verify GET /v3.1/name/india?fullText=true returns 200 with a non-empty country array
**Feature:** Countries by Name API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path request succeeds, returns JSON, and the body is a non-empty array of country objects.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke

### TC-294 — Confirm GET /v3.1/name/india returns the requested fields with name as object, population as number and region as string
**Feature:** Countries by Name API · **Priority:** P0 · **Type:** data · **Severity:** Major

Asserts the field-projected response carries the exact fields requested via the fields parameter, with correct types on the first array element.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.name" is of type object. → Expected: The JSON field "0.name" is present and is of type object.
4. Assert that JSON "0.capital" is of type array. → Expected: The JSON field "0.capital" is present and is of type array.
5. Assert that JSON "0.population" is of type number. → Expected: The JSON field "0.population" is present and is of type number.
6. Assert that JSON "0.currencies" is of type object. → Expected: The JSON field "0.currencies" is present and is of type object.
7. Assert that JSON "0.region" is of type string. → Expected: The JSON field "0.region" is present and is of type string.

**Expected Result:** All 6 assertions pass — response status is 200; JSON "0.name" is of type object; JSON "0.capital" is of type array; JSON "0.population" is of type number; JSON "0.currencies" is of type object; JSON "0.region" is of type string.

**Tags:** api, schema

### TC-295 — Return 404 for GET /v3.1/name/zzzznotacountry when the country name cannot be matched
**Feature:** Countries by Name API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A syntactically valid but non-existent country name must yield a 404 Not Found rather than a 2xx or a server error.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/zzzznotacountry?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/zzzznotacountry?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative

### TC-296 — Return 404 for GET /v3.1/name/india?fullText=true when the full-text flag excludes the partial match set
**Feature:** Countries by Name API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Requesting an exact-name match against a value that is not an exact official/common country name must return 404, exercising the fullText filter behaviour.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/ind?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/ind?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, filter

### TC-297 — Confirm GET /v3.1/name/india ignores an unknown query parameter and still returns 200
**Feature:** Countries by Name API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unrecognised query parameter alongside the documented request and verifies the API safely ignores it rather than failing with a 5xx.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region&bogusParam=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region&bogusParam=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.

**Expected Result:** All 2 assertions pass — response status is 200; JSON body is a non-empty array.

**Tags:** api, edge

### TC-298 — Verify GET /api/v2/pokemon/pikachu returns 200 with a non-empty JSON body
**Feature:** Pokemon API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path request succeeds, responds as JSON within a reasonable time, and carries a populated body.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/pikachu is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/pikachu. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "name" is present. → Expected: The JSON field "name" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "name" is present; response time is under 5000 ms.

**Tags:** api, smoke

### TC-299 — Confirm GET /api/v2/pokemon/pikachu returns id as a number and name equal to "pikachu"
**Feature:** Pokemon API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the key identifying fields of the returned resource carry the correct types and the name matches the requested identifier.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/pikachu is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/pikachu. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
4. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
5. Assert that JSON "name" is of type string. → Expected: The JSON field "name" is present and is of type string.
6. Assert that JSON "name" equals "pikachu". → Expected: The JSON field "name" is present and equals "pikachu".

**Expected Result:** All 5 assertions pass — response status is 200; JSON "id" is present; JSON "id" is of type number; JSON "name" is of type string; JSON "name" equals "pikachu".

**Tags:** api, schema

### TC-300 — Return 404 for GET /api/v2/pokemon/99999999 when the numeric id does not exist
**Feature:** Pokemon API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Ensures a syntactically valid but non-existent Pokemon id is rejected as not found rather than returning a stale or 5xx response.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative

### TC-301 — Return 404 for GET /api/v2/pokemon/not-a-real-pokemon when the identifier is unrecognised
**Feature:** Pokemon API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Verifies an invalid string identifier that matches no resource yields a clean 404 client error.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/not-a-real-pokemon is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/not-a-real-pokemon. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative

### TC-302 — Verify GET /v2/pet/findByStatus?status=available returns 200 with a non-empty JSON array
**Feature:** Pet FindByStatus API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the happy-path list request succeeds, returns JSON content, responds within a reasonable time and carries at least one pet.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=available. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke

### TC-303 — Confirm GET /v2/pet/findByStatus?status=available returns pet objects whose status field equals 'available'
**Feature:** Pet FindByStatus API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates that the filter is honoured — the first returned pet exposes an id (number) and a status matching the requested value.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=available. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.id" is of type number. → Expected: The JSON field "0.id" is present and is of type number.
4. Assert that JSON "0.status" equals "available". → Expected: The JSON field "0.status" is present and equals "available".

**Expected Result:** All 3 assertions pass — response status is 200; JSON "0.id" is of type number; JSON "0.status" equals "available".

**Tags:** api, schema

### TC-304 — Reject GET /v2/pet/findByStatus with 400 when the status value is invalid
**Feature:** Pet FindByStatus API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends an unrecognised status filter value and expects the API to reject it rather than return a 5xx.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=not_a_real_status is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=not_a_real_status. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 404. → Expected: The response status code is one of 400, 404.

**Expected Result:** All 1 assertion pass — response status is one of 400, 404.

**Tags:** api, negative

### TC-305 — Reject GET /v2/pet/findByStatus with 400 when the required status query parameter is omitted
**Feature:** Pet FindByStatus API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Calls the list endpoint without the mandatory status filter and expects a client error, not a server error.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 405. → Expected: The response status code is one of 400, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 405; response is an error (non-2xx).

**Tags:** api, negative

### TC-306 — Confirm GET /v2/pet/findByStatus?status=available ignores an unknown query parameter and still returns 200
**Feature:** Pet FindByStatus API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unrecognised query parameter alongside the valid status filter and verifies the endpoint tolerates it rather than failing.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available&foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=available&foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.

**Expected Result:** All 2 assertions pass — response status is 200; JSON body is a non-empty array.

**Tags:** api, edge

### TC-307 — Reject POST /v2/pet/findByStatus?status=available with 405 because only GET is allowed
**Feature:** Pet FindByStatus API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends a POST against the read-only list URL and expects the method to be rejected with a documented client error.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet/findByStatus?status=available. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 405, 404, 415. → Expected: The response status code is one of 405, 404, 415.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 405, 404, 415; response is an error (non-2xx).

**Tags:** api, negative

### TC-308 — Verify POST /v2/pet creates a pet and returns 200 with a JSON body
**Feature:** Pet API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the fully-populated documented pet payload and confirms the create endpoint responds successfully with a JSON content-type and a non-empty body.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":987654321,"category":{"id":1,"name":"Dogs"},"name":"QA-Doggo","photoUrls":["https://example.com/dog.png"],"tags":[{"id":1,"name":"automation"}],"status":"available"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, create

### TC-309 — Confirm POST /v2/pet echoes id as a number and name and status as strings matching the request
**Feature:** Pet API · **Priority:** P0 · **Type:** data · **Severity:** Major

The create endpoint returns the persisted pet; verify the key fields from the submitted body are present with the correct types and values.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":987654321,"category":{"id":1,"name":"Dogs"},"name":"QA-Doggo","photoUrls":["https://example.com/dog.png"],"tags":[{"id":1,"name":"automation"}],"status":"available"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "id" equals 987654321. → Expected: The JSON field "id" is present and equals 987654321.
5. Assert that JSON "name" is of type string. → Expected: The JSON field "name" is present and is of type string.
6. Assert that JSON "name" equals "QA-Doggo". → Expected: The JSON field "name" is present and equals "QA-Doggo".
7. Assert that JSON "status" is of type string. → Expected: The JSON field "status" is present and is of type string.
8. Assert that JSON "photoUrls" is of type array. → Expected: The JSON field "photoUrls" is present and is of type array.

**Expected Result:** All 7 assertions pass — response status is 200; JSON "id" is of type number; JSON "id" equals 987654321; JSON "name" is of type string; JSON "name" equals "QA-Doggo"; JSON "status" is of type string; JSON "photoUrls" is of type array.

**Tags:** api, schema, create

### TC-310 — Reject POST /v2/pet with 400 when the request body is malformed JSON
**Feature:** Pet API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Submits a syntactically broken JSON body and confirms the endpoint rejects it with a 400 rather than a server error.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":987654321,"name":"QA-Doggo","status":"available"] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, validation

### TC-311 — Reject POST /v2/pet with a 4xx when the id field is a string instead of a number
**Feature:** Pet API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends the pet payload with a wrongly-typed id (string) and confirms the endpoint returns a client error rather than accepting or 500-ing.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":"not-a-number","category":{"id":1,"name":"Dogs"},"name":"QA-Doggo","photoUrls":["https://example.com/dog.png"],"status":"available"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 405, 422, 500. → Expected: The response status code is one of 400, 405, 422, 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 405, 422, 500; response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-312 — Reject GET /v2/pet with 405 because the create endpoint only accepts POST
**Feature:** Pet API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Issues a GET against the create URL to confirm an unsupported method returns a 405 (or documented 4xx) and never a 2xx.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 404, 405; response is an error (non-2xx).

**Tags:** api, negative, method

### TC-313 — Verify GET /v2/store/inventory returns 200 with a JSON inventory object
**Feature:** Store Inventory API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the inventory list endpoint responds successfully over the documented request and returns a JSON body representing the stock map keyed by pet status.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "root" is present. → Expected: The JSON field "the response root" is present in the response body.
5. Assert that JSON "root" is of type object. → Expected: The JSON field "the response root" is present and is of type object.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "root" is present; JSON "root" is of type object.

**Tags:** api, smoke, inventory

### TC-314 — Confirm GET /v2/store/inventory returns 200 within 5000ms on the happy path
**Feature:** Store Inventory API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Guards the response time of the inventory endpoint so a slow-but-successful response is caught before release.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, inventory

### TC-315 — Confirm GET /v2/store/inventory ignores an unknown query parameter and still returns 200
**Feature:** Store Inventory API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Sends a bogus query parameter that the inventory endpoint does not define and verifies it is safely ignored with a 2xx response rather than causing a server error.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory?bogus=abc123 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory?bogus=abc123. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, negative, inventory

### TC-316 — Reject POST /v2/store/inventory with a 4xx/405 since the endpoint only supports GET
**Feature:** Store Inventory API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Issues a POST against the read-only inventory URL to confirm an unsupported write method is refused rather than accepted or throwing a 500.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 404, 405. → Expected: The response status code is one of 400, 404, 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 404, 405.

**Tags:** api, negative, method

### TC-317 — Confirm GET /v2/store/inventory returns identical 200 status on two consecutive calls (idempotency)
**Feature:** Store Inventory API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Verifies the read-only inventory endpoint is idempotent by asserting a successful 200 that is stable across repeated identical requests.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, idempotency, inventory

### TC-318 — Verify GET /headers returns 200 with a JSON content-type on the documented request
**Feature:** Headers API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the headers endpoint responds successfully and returns JSON when called with the documented X-QA-Team header.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "headers" is present. → Expected: The JSON field "headers" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "headers" is present.

**Tags:** api, smoke

### TC-319 — Confirm GET /headers returns the headers property as a JSON object echoing the request headers
**Feature:** Headers API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the response body carries a top-level 'headers' object, matching the documented httpbin schema.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "headers" is of type object. → Expected: The JSON field "headers" is present and is of type object.
4. Assert that JSON "headers.X-Qa-Team" equals "Automation". → Expected: The JSON field "headers.X-Qa-Team" is present and equals "Automation".

**Expected Result:** All 3 assertions pass — response status is 200; JSON "headers" is of type object; JSON "headers.X-Qa-Team" equals "Automation".

**Tags:** api, schema

### TC-320 — Verify GET /headers responds within 5000ms on the happy path
**Feature:** Headers API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Guards the response latency of the headers endpoint under the documented request.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-321 — Reject POST /headers with 405 because only GET is supported on the headers resource
**Feature:** Headers API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a POST to the GET-only headers endpoint and expects a Method Not Allowed rather than success.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 405; response is an error (non-2xx).

**Tags:** api, negative, method

### TC-322 — Verify GET /headers ignores an unknown query parameter and still returns 200
**Feature:** Headers API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms an unexpected query parameter does not break the endpoint or cause a 500 error.

**Precondition:** The API endpoint https://httpbin.org/headers?foo=bar&limit=10 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers?foo=bar&limit=10. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "headers" is present. → Expected: The JSON field "headers" is present in the response body.
4. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "headers" is present; response status is under 500.

**Tags:** api, edge, query

### TC-323 — Verify GET /basic-auth/qauser/qapass returns 200 with a JSON body when valid credentials are supplied
**Feature:** qapass Basic Auth API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the protected basic-auth endpoint responds successfully and returns a JSON payload on the documented happy path.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "authenticated" is present. → Expected: The JSON field "authenticated" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "authenticated" is present; response time is under 5000 ms.

**Tags:** api, smoke, auth

### TC-324 — Confirm GET /basic-auth/qauser/qapass returns authenticated as a boolean and user as a string on success
**Feature:** qapass Basic Auth API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the schema of the basic-auth success response by asserting the type of the documented top-level fields.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "authenticated" is of type boolean. → Expected: The JSON field "authenticated" is present and is of type boolean.
4. Assert that JSON "user" is of type string. → Expected: The JSON field "user" is present and is of type string.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "authenticated" is of type boolean; JSON "user" is of type string.

**Tags:** api, schema

### TC-325 — Return 401 for GET /basic-auth/qauser/qapass when no credentials are supplied
**Feature:** qapass Basic Auth API · **Priority:** P0 · **Type:** security · **Severity:** Critical

Ensures the basic-auth endpoint rejects unauthenticated requests instead of leaking the protected resource.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 401, 403. → Expected: The response status code is one of 401, 403.

**Expected Result:** All 1 assertion pass — response status is one of 401, 403.

**Tags:** api, auth, negative

### TC-326 — Return 401 for GET /basic-auth/qauser/wrongpass when the password segment does not match the required credential
**Feature:** qapass Basic Auth API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests a basic-auth path whose expected password differs from the supplied credential and confirms access is denied.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/wrongpass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/wrongpass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 401, 403. → Expected: The response status code is one of 401, 403.

**Expected Result:** All 1 assertion pass — response status is one of 401, 403.

**Tags:** api, auth, negative

### TC-327 — Reject POST /basic-auth/qauser/qapass with a 4xx/405 since only GET is supported on this resource
**Feature:** qapass Basic Auth API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unsupported HTTP method against the basic-auth URL and confirms it is refused rather than returning a server error.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-328 — Verify GET /status/500 returns the configured 200 status on the documented request
**Feature:** Status API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented GET request to the status endpoint and confirms it responds with the expected 200 status, carries a content-type header, and answers within the performance budget.

**Precondition:** The API endpoint https://httpbin.org/status/500 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/500. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "/"; response time is under 5000 ms.

**Tags:** api, smoke, status

### TC-329 — Confirm GET /status/500 responds successfully and includes a content-type header on the happy path
**Feature:** Status API · **Priority:** P1 · **Type:** data · **Severity:** Major

Since no sample body is documented, this case asserts contract resilience: the endpoint returns a 2xx and always advertises its media type via the content-type header rather than a bare or error response.

**Precondition:** The API endpoint https://httpbin.org/status/500 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/500. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that header "content-type" contains "text". → Expected: The "content-type" response header is present and contains "text".

**Expected Result:** All 2 assertions pass — response is successful (2xx); header "content-type" contains "text".

**Tags:** api, contract, status

### TC-330 — Reject GET /status/abc with a 4xx when a non-numeric status id is supplied
**Feature:** Status API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The status resource expects a numeric id. Requesting a non-numeric path segment must produce a client error (400/404), never a 2xx or a 5xx.

**Precondition:** The API endpoint https://httpbin.org/status/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 404. → Expected: The response status code is one of 400, 404.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 404.

**Tags:** api, validation, status

### TC-331 — Return a non-2xx for GET /status/99999999 when an out-of-range status id cannot resolve
**Feature:** Status API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Supplies an implausibly large status id that does not map to any real HTTP status code and asserts the endpoint declines it with a non-successful response rather than echoing a 200.

**Precondition:** The API endpoint https://httpbin.org/status/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, status

### TC-332 — Verify GET https://httpbin.org/delay/2 returns 200 with a JSON body after the delay
**Feature:** Delay API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the delay endpoint responds successfully with a JSON payload once the configured delay elapses.

**Precondition:** The API endpoint https://httpbin.org/delay/2 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "url" is present. → Expected: The JSON field "url" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "url" is present.

**Tags:** api, smoke

### TC-333 — Confirm GET https://httpbin.org/delay/2 returns the echoed url as a string and a headers object
**Feature:** Delay API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the shape of the httpbin delay response: url is a string and headers is an object.

**Precondition:** The API endpoint https://httpbin.org/delay/2 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "url" is of type string. → Expected: The JSON field "url" is present and is of type string.
4. Assert that JSON "headers" is of type object. → Expected: The JSON field "headers" is present and is of type object.
5. Assert that response body contains "httpbin.org/delay/2". → Expected: The response body contains the text "httpbin.org/delay/2".

**Expected Result:** All 4 assertions pass — response status is 200; JSON "url" is of type string; JSON "headers" is of type object; response body contains "httpbin.org/delay/2".

**Tags:** api, schema

### TC-334 — Verify GET https://httpbin.org/delay/2 completes under 8000ms as a response-time guard
**Feature:** Delay API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Guards the happy path against regressions: the 2-second delay endpoint should still respond well under the 8s ceiling.

**Precondition:** The API endpoint https://httpbin.org/delay/2 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 8000 ms. → Expected: The endpoint responds in under 8000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 8000 ms.

**Tags:** api, performance

### TC-335 — Confirm GET https://httpbin.org/delay/2 ignores an unknown query parameter and still returns 200
**Feature:** Delay API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Sends an unrecognised query parameter and expects the endpoint to ignore it and echo it under args rather than error.

**Precondition:** The API endpoint https://httpbin.org/delay/2?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "args.foo" equals "bar". → Expected: The JSON field "args.foo" is present and equals "bar".

**Expected Result:** All 2 assertions pass — response status is 200; JSON "args.foo" equals "bar".

**Tags:** api, negative

### TC-336 — Reject POST https://httpbin.org/delay/2 with a 405 when using the wrong HTTP method
**Feature:** Delay API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The delay resource exposes read only; a POST against the same URL must not succeed and should return a 405.

**Precondition:** The API endpoint https://httpbin.org/delay/2 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://httpbin.org/delay/2. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 405; response is an error (non-2xx).

**Tags:** api, negative

### TC-337 — Reject GET https://httpbin.org/delay/abc with a 4xx when the delay value is non-numeric
**Feature:** Delay API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The delay path segment must be numeric; a non-numeric value should be refused rather than accepted or 500.

**Precondition:** The API endpoint https://httpbin.org/delay/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 404. → Expected: The response status code is one of 400, 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 404; response is an error (non-2xx).

**Tags:** api, negative

### TC-338 — Verify the full posts lifecycle: create → read → update → verify → delete → confirm gone
**Feature:** posts flows · **Priority:** P0 · **Type:** e2e · **Severity:** Critical

Creates a post, reads it back by the returned id, updates its title and body, verifies the update persisted, deletes the post and confirms the id is gone.

**Precondition:** The 6 endpoints in this flow are reachable and the environment allows creating and deleting posts records.

**Steps:**

1. Step 1 (create): send POST https://jsonplaceholder.typicode.com/posts with the step's JSON body. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":1}] → Expected: The response status code is one of 200, 201. The JSON field "id" is present in the response body. The JSON field "title" is present and equals "QA Automation Post". The JSON field "userId" is present and equals 1. Captures "postId" from "id" for the following steps.
2. Step 2 (read): send GET https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is exactly 200. The JSON field "id" is present in the response body.
3. Step 3 (update): send PUT https://jsonplaceholder.typicode.com/posts/{{postId}} with the step's JSON body, filling {{…}} from earlier steps. [data: {"id":{{postId}},"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The response status code is exactly 200. The JSON field "title" is present and equals "Updated Title". The JSON field "body" is present and equals "Updated body". The JSON field "userId" is present and equals 1.
4. Step 4 (verify-update): send GET https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is exactly 200. The JSON field "id" is present in the response body.
5. Step 5 (delete): send DELETE https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is exactly 200.
6. Step 6 (confirm-gone): send GET https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is one of 404, 200.

**Expected Result:** All 14 assertions pass — create: response status is one of 200, 201, JSON "id" is present, JSON "title" equals "QA Automation Post", JSON "userId" equals 1; read: response status is 200, JSON "id" is present; update: response status is 200, JSON "title" equals "Updated Title", JSON "body" equals "Updated body", JSON "userId" equals 1; verify-update: response status is 200, JSON "id" is present; delete: response status is 200; confirm-gone: response status is one of 404, 200.

**Tags:** api, flow, regression, posts
