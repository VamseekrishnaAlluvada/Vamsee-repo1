# Test Cases

Generated by JBS IntelliQE — 166 test case(s).

| Module | Cases |
|---|---|
| API | 166 |

## API

### TC-001 — Verify GET /posts returns 200 with a non-empty JSON array of posts
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the list endpoint responds successfully, returns application/json and a non-empty array body within an acceptable time.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-002 — Confirm the first element of GET /posts exposes a numeric id and string title
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the shape of a list item so downstream consumers can rely on the documented post structure.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.id" is present. → Expected: The JSON field "0.id" is present in the response body.
4. Assert that JSON "0.id" is of type number. → Expected: The JSON field "0.id" is present and is of type number.
5. Assert that JSON "0.title" is of type string. → Expected: The JSON field "0.title" is present and is of type string.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "0.id" is present; JSON "0.id" is of type number; JSON "0.title" is of type string.

**Tags:** api, schema, posts

### TC-003 — Return 404 for GET /posts/99999999 when the requested post id does not exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Ensures a read against a non-existent post id on the same resource yields a clean not-found rather than a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, posts

### TC-004 — Reject GET /posts/abc with a 4xx when a non-numeric id is supplied where a number is required
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

The id parameter is numeric; a non-numeric path segment must not resolve to a valid post and must not return a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation, posts

### TC-005 — Confirm GET /posts ignores an unknown query parameter and still returns 200 with a JSON array
**Feature:** Posts API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unrecognised filter parameter should be safely ignored, returning the list successfully rather than triggering a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts?bogusParam=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts?bogusParam=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array.

**Tags:** api, edge, query, posts

### TC-006 — Verify GET /posts/1 returns 200 with a non-empty JSON post object
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented read request succeeds, returns JSON content-type and carries a body with a plausible top-level identifier field.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present.

**Tags:** api, smoke, posts

### TC-007 — Confirm GET /posts/1 returns id as a number within a 5s response-time budget
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the identifier field type on the happy-path response and guards the response time on the read path.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "id" is of type number; response time is under 5000 ms.

**Tags:** api, schema, posts

### TC-008 — Return 404 for GET /posts/99999999 when the post id does not exist
**Feature:** Posts API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Requests a well-formed but non-existent post id and expects a not-found response rather than a success or server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, posts

### TC-009 — Reject GET /posts/not-a-number with a 4xx when a non-numeric id is supplied
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a non-numeric value where the id path parameter expects a number and expects a client-error response, not a 2xx or 500.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-010 — Confirm GET /posts/1 ignores an unknown query parameter and still returns 200
**Feature:** Posts API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unrecognised query parameter to the read request and verifies it is safely ignored rather than causing a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/1?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present.

**Tags:** api, edge, posts

### TC-011 — Verify GET /comments?postId=1 returns 200 with a non-empty JSON array of comments
**Feature:** Comments API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the list endpoint responds successfully for the documented filter, returns a JSON content-type and a non-empty array body within an acceptable time.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke, comments

### TC-012 — Confirm GET /comments?postId=1 returns comments whose postId equals 1 and carry a numeric id
**Feature:** Comments API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the filter is honoured: the first element of the returned array has postId equal to the requested value and exposes a numeric id.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.postId" equals 1. → Expected: The JSON field "0.postId" is present and equals 1.
4. Assert that JSON "0.id" is of type number. → Expected: The JSON field "0.id" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "0.postId" equals 1; JSON "0.id" is of type number.

**Tags:** api, schema, comments

### TC-013 — Verify GET /comments?postId=99999999 returns 200 with an empty array for a postId that matches no comments
**Feature:** Comments API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Documents the empty-result behaviour: filtering by a postId that cannot exist still returns 200 with a JSON array body rather than an error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "root" is of type array.

**Tags:** api, edge, comments

### TC-014 — Verify GET /comments?postId=abc handles a non-numeric postId cleanly without a 5xx error
**Feature:** Comments API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends a non-numeric value where a numeric postId is expected and asserts the API degrades gracefully with a client-level (non-5xx) status rather than crashing.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is under 500; header "content-type" contains "application/json".

**Tags:** api, negative, comments

### TC-015 — Confirm GET /comments?postId=1 ignores an unknown query parameter and still returns 200
**Feature:** Comments API · **Priority:** P3 · **Type:** negative · **Severity:** Minor

Adds an unrecognised query parameter alongside the valid filter and asserts the endpoint tolerates it, returning a successful 2xx JSON array rather than an error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/comments?postId=1&bogusParam=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/comments?postId=1&bogusParam=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.

**Expected Result:** All 2 assertions pass — response is successful (2xx); JSON body is a non-empty array.

**Tags:** api, negative, comments

### TC-016 — Verify POST /posts creates a resource and returns 200 with a JSON body
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented create payload and confirms the endpoint responds with the configured success status, a JSON content-type, and a non-empty body.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "json". → Expected: The "content-type" response header is present and contains "json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "json"; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-017 — Confirm POST /posts returns a generated id property on the created resource
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the create response carries a top-level id so dependent read/update/delete operations have a usable identifier.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "id" is present.

**Tags:** api, schema, posts

### TC-018 — Reject POST /posts with a 4xx when the required title field is missing
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Drops the title field from the documented payload and expects the create request to be refused rather than accepted.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"body":"Created from Postman collection","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, validation, posts

### TC-019 — Return 400 for POST /posts when the request body is malformed JSON
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON body and expects the server to reject it with a client error rather than a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":1] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 415, 422. → Expected: The response status code is one of 400, 415, 422.

**Expected Result:** All 1 assertion pass — response status is one of 400, 415, 422.

**Tags:** api, negative, malformed, posts

### TC-020 — Reject POST /posts with a 4xx when userId is a string instead of a number
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Submits a wrong-typed userId to confirm type validation rejects the create request rather than storing invalid data.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://jsonplaceholder.typicode.com/posts carrying the JSON request body shown above. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":"one"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, validation, posts

### TC-021 — Verify PUT /posts/1 returns 200 with a JSON body when updating the post with the documented payload
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented update payload to /posts/1 and confirms the endpoint responds with exactly 200, a JSON content-type, and a non-empty body.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, posts, update

### TC-022 — Confirm PUT /posts/1 echoes id as a number and title as a string after a successful update
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the updated resource returned by the API carries the key fields from the request payload with the correct types and the id preserved as 1.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "id" equals 1. → Expected: The JSON field "id" is present and equals 1.
5. Assert that JSON "title" is of type string. → Expected: The JSON field "title" is present and is of type string.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "id" is of type number; JSON "id" equals 1; JSON "title" is of type string.

**Tags:** api, schema, posts

### TC-023 — Reject PUT /posts/1 with a 4xx when the request body is syntactically malformed JSON
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a broken JSON payload with the JSON content-type to confirm the endpoint refuses it with a client error rather than a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title",] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, posts, validation

### TC-024 — Reject PUT /posts/1 with a 4xx when a non-numeric id path segment is used
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Targets /posts/abc where a numeric id is required to confirm the malformed path is rejected with a client error and never a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/abc carrying the JSON request body shown above. [data: {"id":1,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, posts, path

### TC-025 — Return 404 for PUT /posts/99999999 when updating a post id that does not exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Attempts to update a non-existent post id to confirm the endpoint responds with 404 rather than silently succeeding.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://jsonplaceholder.typicode.com/posts/99999999 carrying the JSON request body shown above. [data: {"id":99999999,"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, posts, not-found

### TC-026 — Verify PATCH /posts/1 with a valid title returns 200 and a JSON body
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented partial update and confirms the endpoint responds successfully with a JSON content-type and a non-empty body.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"title": "Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-027 — Confirm PATCH /posts/1 echoes id as a number and the updated title as a string
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the response schema of the patched resource: the id remains numeric and the returned title is a string reflecting the update.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"title": "Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "id" equals 1. → Expected: The JSON field "id" is present and equals 1.
5. Assert that JSON "title" is of type string. → Expected: The JSON field "title" is present and is of type string.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "id" is of type number; JSON "id" equals 1; JSON "title" is of type string.

**Tags:** api, schema, posts

### TC-028 — Reject PATCH /posts/1 with 400 when the request body is malformed JSON
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON payload and expects the server to refuse it rather than accept or error with 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/1 carrying the JSON request body shown above. [data: {"title": "Patched Title"] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 500. → Expected: The response status code is one of 400, 500.

**Expected Result:** All 1 assertion pass — response status is one of 400, 500.

**Tags:** api, negative, payload

### TC-029 — Return 404 for PATCH /posts/99999999 when the target post does not exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Attempts to patch a post id that cannot exist on the posts resource and expects a not-found response instead of a silent success.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/99999999 carrying the JSON request body shown above. [data: {"title": "Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 404, 500. → Expected: The response status code is one of 404, 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 404, 500.

**Tags:** api, negative, posts

### TC-030 — Reject PATCH /posts/not-a-number with a 4xx when the id is non-numeric
**Feature:** Posts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Uses a non-numeric value where the numeric post id is required and expects a client error rather than a 5xx or an unintended update.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://jsonplaceholder.typicode.com/posts/not-a-number carrying the JSON request body shown above. [data: {"title": "Patched Title"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-031 — Verify DELETE /posts/1 returns 200 and a JSON content-type on the documented happy path
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented delete request for an existing post id and confirms the API responds with the exact expected 200 status, a JSON content-type, and responds within the performance budget.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "json". → Expected: The "content-type" response header is present and contains "json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "json"; response time is under 5000 ms.

**Tags:** api, smoke, delete

### TC-032 — Confirm DELETE /posts/1 returns a present JSON object body after deletion
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Since no sample response body is documented, this asserts only that the delete response carries a JSON object payload (jsonplaceholder returns an empty object) without inventing specific field names.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "root" is of type object. → Expected: The JSON field "the response root" is present and is of type object.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "root" is of type object.

**Tags:** api, schema, delete

### TC-033 — Return a non-2xx status for DELETE /posts/99999999 when the post id does not exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Targets an id far outside the seeded range to verify the API does not report success for a resource that cannot exist.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 200, 404. → Expected: The response status code is one of 200, 404.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response status is one of 200, 404; response status is under 500.

**Tags:** api, negative, not-found

### TC-034 — Reject DELETE /posts/not-a-number with a 4xx when a non-numeric id is supplied
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a non-numeric value where the numeric 'id' path parameter is required and asserts the API responds with a client error rather than a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://jsonplaceholder.typicode.com/posts/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-035 — Verify GET /posts/9999 returns 200 with a JSON content-type and a non-empty body
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented read request succeeds, responds with JSON, and returns a populated body.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/9999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/9999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, posts

### TC-036 — Confirm GET /posts/9999 returns id as a number in the response object
**Feature:** Posts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the primary key field of the post resource is present and typed as a number.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/9999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/9999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "id" is of type number.

**Tags:** api, schema, posts

### TC-037 — Return 404 for GET /posts/99999999 when the post id does not exist
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests a post id far outside the known range and expects a not-found response rather than a server error.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, posts

### TC-038 — Reject GET /posts/not-a-number with a 4xx when a non-numeric id is supplied
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Uses a non-numeric value where a numeric post id is required and expects a client error, never a 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation, posts

### TC-039 — Confirm GET /posts/9999 ignores an unknown query parameter and still returns 200
**Feature:** Posts API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unsupported query parameter to verify the endpoint tolerates it gracefully instead of erroring.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/9999?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/9999?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, edge, posts

### TC-040 — Verify GET /ping returns 200 confirming the service health-check endpoint is up
**Feature:** Ping API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the ping health-check endpoint responds successfully with the expected status code on the documented request.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text". → Expected: The "content-type" response header is present and contains "text".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text".

**Tags:** api, smoke, health

### TC-041 — Confirm GET /ping responds within 5000ms so the health-check is fast enough for monitoring
**Feature:** Ping API · **Priority:** P1 · **Type:** edge · **Severity:** Major

Guards the response time of the health-check endpoint on the happy path.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response is successful (2xx); response time is under 5000 ms.

**Tags:** api, performance, health

### TC-042 — Confirm GET /ping returns a non-empty body indicating the service reported its status
**Feature:** Ping API · **Priority:** P1 · **Type:** data · **Severity:** Moderate

Asserts the health-check returns a present, non-empty payload rather than an empty response.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response body contains "Created". → Expected: The response body contains the text "Created".

**Expected Result:** All 2 assertions pass — response status is 200; response body contains "Created".

**Tags:** api, schema, health

### TC-043 — Confirm GET /ping ignores an unknown query parameter and still returns 200 rather than erroring
**Feature:** Ping API · **Priority:** P2 · **Type:** negative · **Severity:** Minor

Sends an unsupported query parameter to verify the health-check ignores it and does not fail with a server error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/ping?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response status is 200; response status is under 500.

**Tags:** api, robustness

### TC-044 — Reject POST /ping with a 4xx/405 because the health-check endpoint only supports GET
**Feature:** Ping API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unsupported HTTP method to the same URL and expects a client error rather than a success.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/ping is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/ping. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-045 — Verify POST /auth with valid admin credentials returns 200 and a token in the body
**Feature:** Auth API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy path: the documented username/password pair authenticates successfully and the response carries an auth token.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin","password":"password123"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "token" is present. → Expected: The JSON field "token" is present in the response body.
5. Assert that JSON "token" is of type string. → Expected: The JSON field "token" is present and is of type string.
6. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 5 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "token" is present; JSON "token" is of type string; response time is under 5000 ms.

**Tags:** api, smoke, auth

### TC-046 — Confirm POST /auth with an invalid password returns 200 and a 'Bad credentials' reason instead of a token
**Feature:** Auth API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Negative credential path: a wrong password must not yield a token; the API surfaces a reason field explaining the rejection.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin","password":"wrong-password"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "reason" is present. → Expected: The JSON field "reason" is present in the response body.
4. Assert that response body contains "Bad credentials". → Expected: The response body contains the text "Bad credentials".

**Expected Result:** All 3 assertions pass — response status is 200; JSON "reason" is present; response body contains "Bad credentials".

**Tags:** api, auth, negative

### TC-047 — Reject POST /auth with a malformed JSON body by returning a 4xx client error
**Feature:** Auth API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A syntactically broken JSON payload must be rejected with a client error and must not crash the service with a 5xx.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin","password":}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, auth, validation

### TC-048 — Confirm POST /auth with the password field omitted does not issue a token and returns a 'Bad credentials' reason
**Feature:** Auth API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Dropping the required password field must fail authentication rather than granting access; the response body carries the rejection reason.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/auth carrying the JSON request body shown above. [data: {"username":"admin"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that JSON "reason" is present. → Expected: The JSON field "reason" is present in the response body.
3. Assert that response body contains "Bad credentials". → Expected: The response body contains the text "Bad credentials".

**Expected Result:** All 2 assertions pass — JSON "reason" is present; response body contains "Bad credentials".

**Tags:** api, auth, validation

### TC-049 — Reject a GET request against POST-only /auth with a 404 or 405 status
**Feature:** Auth API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

The /auth resource only exposes create (POST); using GET on the same URL must return a documented client error, not a token.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/auth is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/auth. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 404, 405; response is an error (non-2xx).

**Tags:** api, auth, method

### TC-050 — Verify POST /booking returns 200 and a JSON body when a complete valid booking is submitted
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Submits the fully documented booking payload and confirms the create endpoint responds successfully with a JSON content-type and a non-empty body.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname": "{{firstname}}", "lastname": "Tester", "totalprice": {{totalprice}}, "depositpaid": true, "bookingdates": {"checkin": "2026-10-01", "checkout": "2026-10-05"}, "additionalneeds": "Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-051 — Confirm POST /booking response exposes a bookingid and the echoed booking object after a successful create
**Feature:** Booking API · **Priority:** P0 · **Type:** data · **Severity:** Critical

Asserts the create response carries a top-level bookingid identifier and a nested booking object wrapping the persisted record.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname": "{{firstname}}", "lastname": "Tester", "totalprice": {{totalprice}}, "depositpaid": true, "bookingdates": {"checkin": "2026-10-01", "checkout": "2026-10-05"}, "additionalneeds": "Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "bookingid" is present. → Expected: The JSON field "bookingid" is present in the response body.
4. Assert that JSON "bookingid" is of type number. → Expected: The JSON field "bookingid" is present and is of type number.
5. Assert that JSON "booking" is present. → Expected: The JSON field "booking" is present in the response body.
6. Assert that JSON "booking" is of type object. → Expected: The JSON field "booking" is present and is of type object.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "bookingid" is present; JSON "bookingid" is of type number; JSON "booking" is present; JSON "booking" is of type object.

**Tags:** api, schema, booking

### TC-052 — Reject POST /booking with a non-2xx status when the required firstname field is missing from the payload
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Drops the mandatory firstname field to confirm the create endpoint refuses an incomplete booking rather than persisting it.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"lastname": "Tester", "totalprice": 150, "depositpaid": true, "bookingdates": {"checkin": "2026-10-01", "checkout": "2026-10-05"}, "additionalneeds": "Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, validation, booking

### TC-053 — Reject POST /booking with a 4xx/5xx status when the request body is syntactically malformed JSON
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a broken JSON body to confirm the create endpoint rejects it with a non-success status instead of accepting garbage input.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname": "Jim", "lastname": "Tester", "totalprice": 150,] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, malformed, booking

### TC-054 — Reject POST /booking with a non-2xx status when totalprice is sent as a string instead of a number
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Supplies a wrong-typed totalprice to confirm the create endpoint enforces the expected numeric type for the price field.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://restful-booker.herokuapp.com/booking carrying the JSON request body shown above. [data: {"firstname": "Jim", "lastname": "Tester", "totalprice": "not-a-number", "depositpaid": true, "bookingdates": {"checkin": "2026-10-01", "checkout": "2026-10-05"}, "additionalneeds": "Breakfast"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, type-validation, booking

### TC-055 — Verify GET /booking returns 200 with a non-empty JSON array of booking ids
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the booking list endpoint responds successfully with JSON content and a populated array of booking references.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-056 — Confirm each element of GET /booking exposes a numeric bookingid property
**Feature:** Booking API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the list schema: every returned entry carries a bookingid field typed as a number.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.bookingid" is present. → Expected: The JSON field "0.bookingid" is present in the response body.
4. Assert that JSON "0.bookingid" is of type number. → Expected: The JSON field "0.bookingid" is present and is of type number.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "0.bookingid" is present; JSON "0.bookingid" is of type number.

**Tags:** api, schema, booking

### TC-057 — Verify GET /booking with a firstname filter returns 200 with a JSON array
**Feature:** Booking API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Confirms the documented firstname filter on the booking list is accepted and yields a JSON array response.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/?firstname=Sally is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/?firstname=Sally. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "root" is of type array.

**Tags:** api, filter, booking

### TC-058 — Confirm GET /booking ignores an unknown query parameter and still returns 200
**Feature:** Booking API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unrecognised query parameter must be safely ignored rather than causing a 500 error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/?bogusparam=xyz123 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking/?bogusparam=xyz123. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 2 assertions pass — response status is under 500; response status is 200.

**Tags:** api, edge, booking

### TC-059 — Reject DELETE against the /booking collection with a non-2xx status
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

The collection URL only supports list and create; a DELETE without an id must not succeed.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, booking

### TC-060 — Verify GET /booking?lastname=Tester returns 200 with a JSON array body
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy-path list request filtered by lastname; confirms the endpoint responds successfully, serves JSON, and returns an array of booking references.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=Tester is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=Tester. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "root" is of type array; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-061 — Verify GET /booking with an unmatched lastname filter returns 200 and a JSON array
**Feature:** Booking API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Filtering by a lastname that is highly unlikely to exist should still succeed with a 200 and an array (empty or not) rather than erroring.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=ZzNoSuchLastname9999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=ZzNoSuchLastname9999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "root" is of type array.

**Tags:** api, booking, filter

### TC-062 — Confirm GET /booking ignores an unknown query parameter and still returns 200
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An unrecognised filter parameter must be safely ignored — the list endpoint should return 200 with a JSON array, never a 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=Tester&bogusparam=42 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=Tester&bogusparam=42. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "root" is of type array.

**Tags:** api, booking, robustness

### TC-063 — Reject DELETE /booking (collection) with a non-2xx status since only GET and POST are supported
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The /booking collection URL exposes list (GET) and create (POST) only; a DELETE against the collection must not succeed.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, method

### TC-064 — Ensure GET /booking with a SQL-injection-shaped lastname returns a clean non-5xx status without echoing the payload
**Feature:** Booking API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Injection probe: a SQL-shaped value in the lastname filter must be handled cleanly (no 500) and the raw injection string must not be reflected in the response body.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking?lastname=Tester%27%20OR%20%271%27%3D%271 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restful-booker.herokuapp.com/booking?lastname=Tester%27%20OR%20%271%27%3D%271. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that JSON "root" is of type array. → Expected: The JSON field "the response root" is present and is of type array.

**Expected Result:** All 2 assertions pass — response status is under 500; JSON "root" is of type array.

**Tags:** api, booking, security, injection

### TC-065 — Verify PUT /booking/ with a complete valid booking payload returns 200 and a JSON body
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented booking update payload and confirms the endpoint responds successfully with a JSON content-type and a non-empty body, within an acceptable response time.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-066 — Reject PUT /booking/ with a 4xx when the required firstname field is missing from the payload
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Omits the mandatory firstname field from the update payload and asserts the API refuses the request rather than accepting a partial booking.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, validation, booking

### TC-067 — Reject PUT /booking/ with a 400 when the request body is malformed JSON
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON body and confirms the endpoint returns a client error instead of a 500 or a silent success.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":500,] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, validation, booking

### TC-068 — Return 404 for PUT /booking/99999999 when updating a booking id that does not exist
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Targets a booking id that cannot exist and confirms the API reports the resource as not found rather than performing an update.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/99999999 carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, booking

### TC-069 — Reject PUT /booking/abc with a 4xx when a non-numeric booking id is supplied in the path
**Feature:** Booking API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Uses a non-numeric identifier where a numeric booking id is expected and confirms the API responds with a client error and does not crash with a 500.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://restful-booker.herokuapp.com/booking/abc carrying the JSON request body shown above. [data: {"firstname":"Updated","lastname":"Tester","totalprice":500,"depositpaid":false,"bookingdates":{"checkin":"2026-11-01","checkout":"2026-11-03"},"additionalneeds":"Lunch"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, validation, booking

### TC-070 — Verify PATCH /booking/ with a valid totalprice returns 200 and a JSON body
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented partial-update payload and confirms the endpoint responds successfully with a JSON content type and a non-empty body.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"totalprice": 999}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "json". → Expected: The "content-type" response header is present and contains "json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "json"; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-071 — Reject PATCH /booking/99999999 with a non-2xx status for a booking id that cannot exist
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Targets an unknown booking id and confirms the API does not report success for a resource that does not exist.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/99999999 carrying the JSON request body shown above. [data: {"totalprice": 999}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, booking

### TC-072 — Reject PATCH /booking/not-a-number with a 4xx when the booking id is non-numeric
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Uses a non-numeric booking id where a numeric id is required and confirms the API returns a client error rather than a server error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/not-a-number carrying the JSON request body shown above. [data: {"totalprice": 999}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-073 — Reject PATCH /booking/ with a 4xx when the request body is empty
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends an empty JSON object carrying no updatable fields and confirms the API rejects the request instead of succeeding.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, payload

### TC-074 — Reject PATCH /booking/ with a 4xx when the JSON body is malformed
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON payload and confirms the API returns a client error rather than crashing with a 5xx.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://restful-booker.herokuapp.com/booking/ carrying the JSON request body shown above. [data: {"totalprice": 999] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, payload

### TC-075 — Verify DELETE /booking/1 with a valid token cookie returns 200 and removes the booking
**Feature:** Booking API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the documented delete request for an existing booking id with the auth token cookie injected, and asserts the exact success status code.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, smoke, booking

### TC-076 — Return 403 for DELETE /booking/1 when no auth token cookie is supplied
**Feature:** Booking API · **Priority:** P0 · **Type:** security · **Severity:** Critical

Confirms the delete operation is protected: without the token credential the API must refuse the request rather than delete the resource.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/1 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/1. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 401, 403. → Expected: The response status code is one of 401, 403.

**Expected Result:** All 1 assertion pass — response status is one of 401, 403.

**Tags:** api, auth, booking, negative

### TC-077 — Reject DELETE /booking/99999999 for a non-existent booking id with a 4xx (not 500)
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Attempts to delete a booking id that cannot exist and asserts the API returns a client error rather than succeeding or crashing.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, negative

### TC-078 — Reject DELETE /booking/not-a-number with a 4xx when a numeric id is required
**Feature:** Booking API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a non-numeric path id where a numeric booking id is expected and asserts the API responds with a client error rather than a server error.

**Precondition:** The API endpoint https://restful-booker.herokuapp.com/booking/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://restful-booker.herokuapp.com/booking/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, booking, negative, validation

### TC-079 — Verify POST /auth/login with valid credentials returns 200 and a JSON session body
**Feature:** Login API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Submits the documented username/password/expiresInMins payload and confirms a successful login with a JSON response body containing the authenticated user context.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","password":"emilyspass","expiresInMins":30}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "username" equals "emilys". → Expected: The JSON field "username" is present and equals "emilys".
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "username" equals "emilys"; response time is under 5000 ms.

**Tags:** api, smoke, auth

### TC-080 — Reject POST /auth/login with 400 when the password is incorrect
**Feature:** Login API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Sends a valid username with a wrong password and confirms the endpoint refuses the login rather than issuing a session.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","password":"wrongpassword","expiresInMins":30}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 400; response is an error (non-2xx).

**Tags:** api, auth, negative

### TC-081 — Reject POST /auth/login with 400 when the required password field is missing
**Feature:** Login API · **Priority:** P0 · **Type:** negative · **Severity:** Major

Drops the password field from the documented body and confirms the endpoint returns a client error instead of authenticating.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","expiresInMins":30}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 401, 422. → Expected: The response status code is one of 400, 401, 422.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 401, 422; response is an error (non-2xx).

**Tags:** api, validation, negative

### TC-082 — Reject POST /auth/login with 400 when the request body is malformed JSON
**Feature:** Login API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a syntactically broken JSON payload and confirms the server rejects it with a client error rather than a 500.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {"username":"emilys","password":"emilyspass",] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 422; response is an error (non-2xx).

**Tags:** api, validation, negative

### TC-083 — Reject POST /auth/login with a client error when an empty JSON object is submitted
**Feature:** Login API · **Priority:** P1 · **Type:** edge · **Severity:** Moderate

Sends an empty object with no credentials and confirms the endpoint returns a 4xx rather than accepting the request or erroring.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/login carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 401, 422. → Expected: The response status code is one of 400, 401, 422.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 401, 422; response is an error (non-2xx).

**Tags:** api, validation, edge

### TC-084 — Reject GET /auth/login with a 4xx/405 because the login endpoint only supports POST
**Feature:** Login API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Issues the wrong HTTP method against the login URL and confirms the server rejects it instead of returning a session.

**Precondition:** The API endpoint https://dummyjson.com/auth/login is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/login. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 404, 405; response is an error (non-2xx).

**Tags:** api, method, negative

### TC-085 — Verify GET /auth/me returns 200 with a JSON content-type on the documented request
**Feature:** Auth Me API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the me endpoint responds successfully and serves a JSON payload for the documented no-parameter request.

**Precondition:** The API endpoint https://dummyjson.com/auth/me is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/me. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, smoke

### TC-086 — Confirm GET /auth/me returns a non-empty JSON body within 5000ms on the happy path
**Feature:** Auth Me API · **Priority:** P1 · **Type:** api · **Severity:** Major

Guards response time and asserts the successful response carries a present, non-empty payload without asserting unprovided field names.

**Precondition:** The API endpoint https://dummyjson.com/auth/me is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/me. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-087 — Reject POST /auth/me with a 4xx because the me resource only supports GET
**Feature:** Auth Me API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unsupported HTTP method against the same URL and expects a client error rather than success or a 500.

**Precondition:** The API endpoint https://dummyjson.com/auth/me is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/auth/me. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, method

### TC-088 — Confirm GET /auth/me ignores an unknown query parameter and still returns 200
**Feature:** Auth Me API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unrecognised query parameter to verify the endpoint tolerates it gracefully instead of failing with a 500.

**Precondition:** The API endpoint https://dummyjson.com/auth/me?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/me?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, query

### TC-089 — Confirm GET /auth/me/ with a trailing slash does not return a 500 server error
**Feature:** Auth Me API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Exercises path variance by appending a trailing slash and asserts the service responds without an internal server error.

**Precondition:** The API endpoint https://dummyjson.com/auth/me/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/auth/me/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, path

### TC-090 — Verify GET /products?limit=10&skip=10&select=title,price returns 200 with a JSON body
**Feature:** Products API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented list request succeeds, responds with JSON, and returns within an acceptable time budget.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=10&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=10&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; response time is under 5000 ms.

**Tags:** api, smoke, products

### TC-091 — Confirm GET /products list returns a non-empty products array with a numeric limit reflecting the paging request
**Feature:** Products API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the paginated list shape: a products collection is present and non-empty and the limit paging field is numeric.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=10&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=10&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "products" is present. → Expected: The JSON field "products" is present in the response body.
4. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
5. Assert that JSON "products" is a non-empty array. → Expected: The "products" field is an array containing at least one item.
6. Assert that JSON "limit" is of type number. → Expected: The JSON field "limit" is present and is of type number.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "products" is present; JSON "products" is of type array; JSON "products" is a non-empty array; JSON "limit" is of type number.

**Tags:** api, schema, pagination

### TC-092 — Reject GET /products with a 4xx when the limit parameter is non-numeric (limit=abc)
**Feature:** Products API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a non-numeric value for the numeric limit paging parameter and expects the API to reject it rather than return 5xx.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=abc&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=abc&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, pagination

### TC-093 — Reject GET /products with a 4xx when limit is negative (limit=-5)
**Feature:** Products API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Supplies a negative limit boundary value and expects a client error, guarding against invalid pagination being silently accepted.

**Precondition:** The API endpoint https://dummyjson.com/products?limit=-5&skip=10&select=title,price is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products?limit=-5&skip=10&select=title,price. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, boundary

### TC-094 — Return 404 for GET /products/99999999 when the product id cannot exist
**Feature:** Products API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests a single product by an id that is well out of range on the products resource and expects a 404 Not Found.

**Precondition:** The API endpoint https://dummyjson.com/products/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, not-found

### TC-095 — Verify GET /products/search?q=phone returns 200 with a JSON body containing the products list
**Feature:** Products Search API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy-path search: the documented request must respond successfully with a JSON content-type and a non-empty result payload.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=phone. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "products" is present. → Expected: The JSON field "products" is present in the response body.
5. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
6. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 5 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "products" is present; JSON "products" is of type array; response time is under 5000 ms.

**Tags:** api, smoke, search

### TC-096 — Confirm GET /products/search?q=phone returns products as an array and pagination fields (total, skip, limit) as numbers
**Feature:** Products Search API · **Priority:** P1 · **Type:** data · **Severity:** Major

Schema check on the search list envelope: the results collection is an array and the pagination metadata are numeric.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=phone. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
4. Assert that JSON "total" is of type number. → Expected: The JSON field "total" is present and is of type number.
5. Assert that JSON "skip" is of type number. → Expected: The JSON field "skip" is present and is of type number.
6. Assert that JSON "limit" is of type number. → Expected: The JSON field "limit" is present and is of type number.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "products" is of type array; JSON "total" is of type number; JSON "skip" is of type number; JSON "limit" is of type number.

**Tags:** api, schema, search

### TC-097 — Verify GET /products/search with a no-match query returns 200 and an empty products array
**Feature:** Products Search API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Edge case: a search term that matches nothing must still succeed with a well-formed empty result set rather than an error.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=zzzznonexistentproductxyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=zzzznonexistentproductxyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.
4. Assert that JSON "total" equals 0. → Expected: The JSON field "total" is present and equals 0.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "products" is of type array; JSON "total" equals 0.

**Tags:** api, edge, search

### TC-098 — Verify GET /products/search?q=phone&limit=5 honours the pagination boundary and returns 200
**Feature:** Products Search API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Pagination boundary: applying a small explicit limit must succeed and keep returning a JSON products array.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone&limit=5 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=phone&limit=5. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "products" is of type array.

**Tags:** api, edge, pagination

### TC-099 — Verify GET /products/search?q=phone&limit=abc with a non-numeric limit does not return a 5xx server error
**Feature:** Products Search API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Malformed query resilience: a non-numeric pagination value must be handled gracefully (rejected or ignored) and never crash the server.

**Precondition:** The API endpoint https://dummyjson.com/products/search?q=phone&limit=abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://dummyjson.com/products/search?q=phone&limit=abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, negative, validation

### TC-100 — Verify POST /carts/add returns 200 with a non-empty JSON body when a valid cart payload is submitted
**Feature:** Carts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Submits the documented userId + products payload and confirms the endpoint creates the cart successfully, returns JSON, responds within the time budget and carries a body.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1,"products":[{"id":144,"quantity":4},{"id":98,"quantity":1}]}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, carts

### TC-101 — Confirm POST /carts/add echoes userId as 1 and returns products as an array on the created cart
**Feature:** Carts API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the created cart reflects the submitted userId literal and exposes a products collection of the correct type.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1,"products":[{"id":144,"quantity":4},{"id":98,"quantity":1}]}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "userId" equals 1. → Expected: The JSON field "userId" is present and equals 1.
4. Assert that JSON "products" is of type array. → Expected: The JSON field "products" is present and is of type array.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "userId" equals 1; JSON "products" is of type array.

**Tags:** api, schema, carts

### TC-102 — Reject POST /carts/add with a 4xx when the required products field is missing from the body
**Feature:** Carts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a payload containing only userId and omitting the required products array; the endpoint must not accept the incomplete cart.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 422.

**Tags:** api, validation, carts

### TC-103 — Return 400 for POST /carts/add when the request body is syntactically malformed JSON
**Feature:** Carts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a body that is not valid JSON to confirm the endpoint rejects it with a client error rather than a 500.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":1,"products":[{"id":144,"quantity":4}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is 400.

**Tags:** api, validation, carts

### TC-104 — Reject POST /carts/add with a 4xx when userId is a string instead of the expected number
**Feature:** Carts API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends a wrong-typed userId to confirm the endpoint validates field types and does not silently accept invalid input.

**Precondition:** The API endpoint https://dummyjson.com/carts/add is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://dummyjson.com/carts/add carrying the JSON request body shown above. [data: {"userId":"one","products":[{"id":144,"quantity":4}]}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 422.

**Tags:** api, validation, carts

### TC-105 — Verify GET /v1/forecast for Mumbai returns 200 with a non-empty JSON forecast body
**Feature:** Forecast API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented current-weather request succeeds, responds with JSON and returns a populated body within a reasonable time.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "json". → Expected: The "content-type" response header is present and contains "json".
4. Assert that JSON "latitude" is present. → Expected: The JSON field "latitude" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "json"; JSON "latitude" is present; response time is under 5000 ms.

**Tags:** api, smoke

### TC-106 — Confirm GET /v1/forecast echoes latitude as a number and returns a current object holding requested variables
**Feature:** Forecast API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the shape of the successful response: coordinates come back numeric and the requested current-weather block is present as an object.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "latitude" is of type number. → Expected: The JSON field "latitude" is present and is of type number.
4. Assert that JSON "longitude" is of type number. → Expected: The JSON field "longitude" is present and is of type number.
5. Assert that JSON "current" is of type object. → Expected: The JSON field "current" is present and is of type object.
6. Assert that JSON "current.temperature_2m" is present. → Expected: The JSON field "current.temperature_2m" is present in the response body.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "latitude" is of type number; JSON "longitude" is of type number; JSON "current" is of type object; JSON "current.temperature_2m" is present.

**Tags:** api, schema

### TC-107 — Reject GET /v1/forecast with a 4xx when latitude is non-numeric (latitude=abc)
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a malformed latitude where a number is required and expects the API to fail the request cleanly rather than return 200 or 500.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 422; response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-108 — Reject GET /v1/forecast with a 4xx when the required latitude parameter is omitted
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Drops the mandatory latitude query parameter and expects a client error, confirming required-parameter validation.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 422. → Expected: The response status code is one of 400, 422.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 422; response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-109 — Confirm GET /v1/forecast still returns 200 when an unknown query parameter is supplied
**Feature:** Forecast API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unrecognised query parameter alongside the valid request to verify the API ignores it rather than failing with a 5xx.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata&foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=19.07&longitude=72.87&current=temperature_2m,relative_humidity_2m,wind_speed_10m&timezone=Asia/Kolkata&foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "json". → Expected: The "content-type" response header is present and contains "json".
4. Assert that JSON "latitude" is present. → Expected: The JSON field "latitude" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "json"; JSON "latitude" is present.

**Tags:** api, edge

### TC-110 — Verify GET /v1/forecast returns 200 with a non-empty JSON body for the documented Bangalore daily query
**Feature:** Forecast API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented forecast request for latitude 12.97 / longitude 77.59 responds successfully with a JSON payload within an acceptable response time.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "daily" is present. → Expected: The JSON field "daily" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "daily" is present; response time is under 5000 ms.

**Tags:** api, smoke, forecast

### TC-111 — Confirm GET /v1/forecast returns latitude as a number and daily as an object for the requested coordinates
**Feature:** Forecast API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the top-level echo fields implied by the request: latitude/longitude are numeric, timezone is a string and the daily container is an object.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=12.97&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "latitude" is of type number. → Expected: The JSON field "latitude" is present and is of type number.
4. Assert that JSON "longitude" is of type number. → Expected: The JSON field "longitude" is present and is of type number.
5. Assert that JSON "timezone" is of type string. → Expected: The JSON field "timezone" is present and is of type string.
6. Assert that JSON "daily" is of type object. → Expected: The JSON field "daily" is present and is of type object.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "latitude" is of type number; JSON "longitude" is of type number; JSON "timezone" is of type string; JSON "daily" is of type object.

**Tags:** api, schema, forecast

### TC-112 — Reject GET /v1/forecast with 4xx when latitude is a non-numeric value
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends latitude=abc where a decimal is required and expects the API to reject the malformed query rather than return a forecast.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=abc&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, validation

### TC-113 — Reject GET /v1/forecast with 400 when the required latitude parameter is missing
**Feature:** Forecast API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Omits the mandatory latitude parameter and expects a client-error response instead of a forecast payload.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, required-param

### TC-114 — Reject GET /v1/forecast with 4xx when latitude 200 is outside the valid -90..90 range
**Feature:** Forecast API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Supplies an out-of-range latitude and expects the API to reject it as an invalid query value rather than returning data.

**Precondition:** The API endpoint https://api.open-meteo.com/v1/forecast?latitude=200&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.open-meteo.com/v1/forecast?latitude=200&longitude=77.59&daily=temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=Asia/Kolkata. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 400. → Expected: The response status code is exactly 400.

**Expected Result:** All 1 assertion pass — response status is 400.

**Tags:** api, negative, boundary

### TC-115 — Verify GET /simple/price with ids=bitcoin,ethereum and vs_currencies=usd,inr returns 200 with a JSON content-type
**Feature:** Simple Price API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path request succeeds, returns a JSON body and responds within an acceptable time budget.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "bitcoin" is present. → Expected: The JSON field "bitcoin" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "bitcoin" is present; response time is under 5000 ms.

**Tags:** api, smoke, price

### TC-116 — Confirm GET /simple/price returns bitcoin.usd and ethereum.inr as numeric price fields for the requested coins and currencies
**Feature:** Simple Price API · **Priority:** P0 · **Type:** data · **Severity:** Critical

Validates that each requested coin id maps to an object whose requested currency keys carry numeric price values, matching the ids and vs_currencies supplied in the query.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "bitcoin.usd" is of type number. → Expected: The JSON field "bitcoin.usd" is present and is of type number.
4. Assert that JSON "bitcoin.inr" is of type number. → Expected: The JSON field "bitcoin.inr" is present and is of type number.
5. Assert that JSON "ethereum.usd" is of type number. → Expected: The JSON field "ethereum.usd" is present and is of type number.
6. Assert that JSON "ethereum.inr" is of type number. → Expected: The JSON field "ethereum.inr" is present and is of type number.

**Expected Result:** All 5 assertions pass — response status is 200; JSON "bitcoin.usd" is of type number; JSON "bitcoin.inr" is of type number; JSON "ethereum.usd" is of type number; JSON "ethereum.inr" is of type number.

**Tags:** api, schema, price

### TC-117 — Reject GET /simple/price with a 4xx when the required vs_currencies parameter is omitted
**Feature:** Simple Price API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Removes the mandatory vs_currencies query parameter and asserts the API rejects the request rather than returning a success or a 500.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-118 — Reject GET /simple/price with a 4xx when the required ids parameter is omitted
**Feature:** Simple Price API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Removes the mandatory ids query parameter and asserts the API rejects the request rather than returning a success or a 500.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-119 — Confirm GET /simple/price returns 200 and ignores an unknown query parameter without erroring
**Feature:** Simple Price API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unsupported query parameter alongside the valid request and asserts the endpoint still succeeds, proving unknown parameters are safely ignored rather than causing a 500.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr&bogusParam=abc123 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr&bogusParam=abc123. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "bitcoin.usd" is of type number. → Expected: The JSON field "bitcoin.usd" is present and is of type number.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "bitcoin.usd" is of type number.

**Tags:** api, edge, robustness

### TC-120 — Reject POST /simple/price with a 4xx because only GET is supported on the price endpoint
**Feature:** Simple Price API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Issues an unsupported HTTP method against the same URL and asserts the API responds with a client error (e.g. 405) rather than accepting the request.

**Precondition:** The API endpoint https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd,inr. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-121 — Verify GET /v3.1/name/india with fullText=true returns 200 and a non-empty JSON array of matching countries
**Feature:** Countries Name Lookup API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy path: the documented full-text name lookup for India responds successfully with a JSON body containing at least one country record.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke, countries

### TC-122 — Confirm GET /v3.1/name/india returns each requested field with the correct type (name object, population number, region string)
**Feature:** Countries Name Lookup API · **Priority:** P1 · **Type:** data · **Severity:** Major

Schema check: with fields=name,capital,population,currencies,region the first record must expose those keys with their documented types.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/india?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0.name" is of type object. → Expected: The JSON field "0.name" is present and is of type object.
4. Assert that JSON "0.capital" is of type array. → Expected: The JSON field "0.capital" is present and is of type array.
5. Assert that JSON "0.population" is of type number. → Expected: The JSON field "0.population" is present and is of type number.
6. Assert that JSON "0.currencies" is of type object. → Expected: The JSON field "0.currencies" is present and is of type object.
7. Assert that JSON "0.region" is of type string. → Expected: The JSON field "0.region" is present and is of type string.

**Expected Result:** All 6 assertions pass — response status is 200; JSON "0.name" is of type object; JSON "0.capital" is of type array; JSON "0.population" is of type number; JSON "0.currencies" is of type object; JSON "0.region" is of type string.

**Tags:** api, schema, countries

### TC-123 — Return 404 for GET /v3.1/name with a non-existent country name so unknown resources are not matched
**Feature:** Countries Name Lookup API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative: a country name that cannot exist must yield a 404 Not Found rather than a match or a server error.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/zzzzzznotacountry?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/zzzzzznotacountry?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, countries

### TC-124 — Return 404 for GET /v3.1/name/india?fullText=true when a numeric path segment cannot match any country name
**Feature:** Countries Name Lookup API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Negative: a malformed, numeric-only name path segment must be rejected with a 404 and must not trigger a 5xx.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/12345?fullText=true&fields=name,capital,population,currencies,region is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/12345?fullText=true&fields=name,capital,population,currencies,region. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, countries

### TC-125 — Reject GET /v3.1/name/india with a 4xx when the fields query parameter lists an invalid field
**Feature:** Countries Name Lookup API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Negative: an unsupported value in the fields filter must produce a client error (4xx) rather than a successful 2xx or a 500.

**Precondition:** The API endpoint https://restcountries.com/v3.1/name/india?fullText=true&fields=notarealfield is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://restcountries.com/v3.1/name/india?fullText=true&fields=notarealfield. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, filter

### TC-126 — Verify GET /api/v2/pokemon/pikachu returns 200 with a non-empty JSON body
**Feature:** Pokemon API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path request succeeds, responds as JSON and returns a populated body.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/pikachu is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/pikachu. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "name" is present. → Expected: The JSON field "name" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "name" is present.

**Tags:** api, smoke

### TC-127 — Confirm GET /api/v2/pokemon/pikachu returns id as a number and name as the string pikachu
**Feature:** Pokemon API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the key top-level fields of the pikachu resource carry the correct types and value.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/pikachu is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/pikachu. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "name" is of type string. → Expected: The JSON field "name" is present and is of type string.
5. Assert that JSON "name" equals "pikachu". → Expected: The JSON field "name" is present and equals "pikachu".

**Expected Result:** All 4 assertions pass — response status is 200; JSON "id" is of type number; JSON "name" is of type string; JSON "name" equals "pikachu".

**Tags:** api, schema

### TC-128 — Verify GET /api/v2/pokemon/pikachu responds within 5000ms on the happy path
**Feature:** Pokemon API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Guards the response time of the documented request to catch latency regressions.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/pikachu is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/pikachu. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-129 — Return 404 for GET /api/v2/pokemon/99999999 when the pokemon id does not exist
**Feature:** Pokemon API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms the API rejects an out-of-range numeric id that cannot map to any pokemon.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative

### TC-130 — Return 404 for GET /api/v2/pokemon/not-a-real-pokemon when the name identifier is unknown
**Feature:** Pokemon API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms an unrecognised name path segment yields a clean 404 rather than a server error.

**Precondition:** The API endpoint https://pokeapi.co/api/v2/pokemon/not-a-real-pokemon is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://pokeapi.co/api/v2/pokemon/not-a-real-pokemon. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative

### TC-131 — Verify GET /v2/pet/findByStatus?status=available returns 200 with a JSON array body
**Feature:** Pet findByStatus API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path request succeeds, returns JSON, responds promptly and yields a non-empty list of pets.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=available. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON body is a non-empty array. → Expected: The response body is an array containing at least one item.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms.

**Tags:** api, smoke, list

### TC-132 — Confirm GET /v2/pet/findByStatus?status=available returns array elements shaped as pet objects
**Feature:** Pet findByStatus API · **Priority:** P1 · **Type:** data · **Severity:** Major

Asserts the first item in the returned list is an object, matching the list-of-pets contract for this endpoint.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=available. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "0" is present. → Expected: The JSON field "0" is present in the response body.
4. Assert that JSON "0" is of type object. → Expected: The JSON field "0" is present and is of type object.

**Expected Result:** All 3 assertions pass — response status is 200; JSON "0" is present; JSON "0" is of type object.

**Tags:** api, schema

### TC-133 — Reject POST against /v2/pet/findByStatus with a 405 Method Not Allowed
**Feature:** Pet findByStatus API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

The list endpoint only supports GET; sending POST to the same URL must be refused rather than accepted or 500.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet/findByStatus?status=available. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 404, 405, 415. → Expected: The response status code is one of 404, 405, 415.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 404, 405, 415.

**Tags:** api, method

### TC-134 — Confirm GET /v2/pet/findByStatus ignores an unknown query parameter and still returns 200
**Feature:** Pet findByStatus API · **Priority:** P2 · **Type:** edge · **Severity:** Minor

Adds an unrecognised query parameter alongside a valid status to verify the endpoint tolerates it rather than failing with a 500.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=available&foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=available&foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, query

### TC-135 — Verify GET /v2/pet/findByStatus with an invalid status value does not return a 5xx error
**Feature:** Pet findByStatus API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Supplies an unsupported status filter value; the endpoint must handle it gracefully (empty list or a clean 4xx) without a server error.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet/findByStatus?status=not_a_real_status is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet/findByStatus?status=not_a_real_status. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, filter, negative

### TC-136 — Verify POST /v2/pet creates a pet and returns 200 with a JSON body
**Feature:** Pet API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Sends the fully documented pet payload and confirms the endpoint responds successfully with a JSON content-type, a non-empty body and an acceptable response time.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":987654321,"category":{"id":1,"name":"Dogs"},"name":"QA-Doggo","photoUrls":["https://example.com/dog.png"],"tags":[{"id":1,"name":"automation"}],"status":"available"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "id" is present. → Expected: The JSON field "id" is present in the response body.
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "id" is present; response time is under 5000 ms.

**Tags:** api, smoke, create

### TC-137 — Confirm POST /v2/pet echoes back the created pet with id as a number and name/status as strings
**Feature:** Pet API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the create response reflects the submitted resource, asserting the field types and literal values that the request body makes unambiguous.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":987654321,"category":{"id":1,"name":"Dogs"},"name":"QA-Doggo","photoUrls":["https://example.com/dog.png"],"tags":[{"id":1,"name":"automation"}],"status":"available"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "id" is of type number. → Expected: The JSON field "id" is present and is of type number.
4. Assert that JSON "name" is of type string. → Expected: The JSON field "name" is present and is of type string.
5. Assert that JSON "name" equals "QA-Doggo". → Expected: The JSON field "name" is present and equals "QA-Doggo".
6. Assert that JSON "status" equals "available". → Expected: The JSON field "status" is present and equals "available".
7. Assert that JSON "photoUrls" is of type array. → Expected: The JSON field "photoUrls" is present and is of type array.

**Expected Result:** All 6 assertions pass — response status is 200; JSON "id" is of type number; JSON "name" is of type string; JSON "name" equals "QA-Doggo"; JSON "status" equals "available"; JSON "photoUrls" is of type array.

**Tags:** api, schema, create

### TC-138 — Reject POST /v2/pet with a 4xx when the request body is syntactically malformed JSON
**Feature:** Pet API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a broken JSON payload and confirms the API refuses it with a client error rather than a 5xx.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":987654321,"name":"QA-Doggo","status":"available"] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 415, 422. → Expected: The response status code is one of 400, 415, 422.

**Expected Result:** All 1 assertion pass — response status is one of 400, 415, 422.

**Tags:** api, negative, validation

### TC-139 — Reject POST /v2/pet with a 4xx when the id field carries a string instead of a number
**Feature:** Pet API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Submits an otherwise valid pet with a wrong-typed id to confirm the endpoint validates the field type and returns a client error, not a 5xx.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/pet carrying the JSON request body shown above. [data: {"id":"not-a-number","category":{"id":1,"name":"Dogs"},"name":"QA-Doggo","photoUrls":["https://example.com/dog.png"],"tags":[{"id":1,"name":"automation"}],"status":"available"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 400, 405, 422, 500. → Expected: The response status code is one of 400, 405, 422, 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 400, 405, 422, 500.

**Tags:** api, negative, validation

### TC-140 — Return 405 for GET against POST-only /v2/pet create endpoint
**Feature:** Pet API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Issues a GET on the create URL to confirm the wrong HTTP method is rejected with a method-not-allowed (or documented 4xx) rather than succeeding.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/pet is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/pet. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 404, 405.

**Tags:** api, negative, method

### TC-141 — Verify GET /v2/store/inventory returns 200 with a JSON content-type
**Feature:** Inventory API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy-path smoke test confirming the inventory endpoint responds successfully and serves JSON.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, smoke, inventory

### TC-142 — Confirm GET /v2/store/inventory returns a non-empty JSON object body
**Feature:** Inventory API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates that the inventory response payload is present and is a JSON object mapping status names to counts.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "root" is of type object. → Expected: The JSON field "the response root" is present and is of type object.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "root" is of type object.

**Tags:** api, schema, inventory

### TC-143 — Verify GET /v2/store/inventory responds within 5000 ms on the happy path
**Feature:** Inventory API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Performance guard ensuring the inventory lookup completes inside the agreed response-time budget.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, inventory

### TC-144 — Ignore an unknown query parameter on GET /v2/store/inventory and still return 200
**Feature:** Inventory API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Confirms that an unrecognised query parameter is safely ignored rather than causing a server error.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://petstore.swagger.io/v2/store/inventory?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, negative, inventory

### TC-145 — Reject POST /v2/store/inventory with a 405 because only GET is supported
**Feature:** Inventory API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends an unsupported HTTP method to the inventory URL and asserts the request is not accepted as successful.

**Precondition:** The API endpoint https://petstore.swagger.io/v2/store/inventory is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://petstore.swagger.io/v2/store/inventory. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, method-not-allowed

### TC-146 — Verify GET /headers returns 200 with a JSON content-type on the documented request
**Feature:** Headers API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the headers endpoint responds successfully with the configured X-QA-Team header and returns a JSON payload.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "headers" is present. → Expected: The JSON field "headers" is present in the response body.
5. Assert that JSON "headers" is of type object. → Expected: The JSON field "headers" is present and is of type object.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "headers" is present; JSON "headers" is of type object.

**Tags:** api, smoke

### TC-147 — Confirm GET /headers echoes the X-QA-Team request header back in the response body
**Feature:** Headers API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the endpoint reflects the sent X-QA-Team header value so header propagation can be validated.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "headers.X-Qa-Team" equals "Automation". → Expected: The JSON field "headers.X-Qa-Team" is present and equals "Automation".
4. Assert that response body contains "Automation". → Expected: The response body contains the text "Automation".

**Expected Result:** All 3 assertions pass — response status is 200; JSON "headers.X-Qa-Team" equals "Automation"; response body contains "Automation".

**Tags:** api, schema

### TC-148 — Verify GET /headers responds within 5000ms on the happy path
**Feature:** Headers API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Guards the response time of the documented request to catch latency regressions.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-149 — Confirm GET /headers ignores an unknown query parameter and still returns 200
**Feature:** Headers API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Sends an unsupported query parameter to ensure it is safely ignored rather than causing a server error.

**Precondition:** The API endpoint https://httpbin.org/headers?foo=bar&limit=9999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/headers?foo=bar&limit=9999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "headers" is present. → Expected: The JSON field "headers" is present in the response body.

**Expected Result:** All 2 assertions pass — response status is 200; JSON "headers" is present.

**Tags:** api, edge

### TC-150 — Reject POST /headers with 405 because the endpoint only supports GET
**Feature:** Headers API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a POST to the read-only headers endpoint and expects a Method Not Allowed response.

**Precondition:** The API endpoint https://httpbin.org/headers is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://httpbin.org/headers. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 405; response is an error (non-2xx).

**Tags:** api, negative

### TC-151 — Verify GET /basic-auth/qauser/qapass returns 200 with a non-empty JSON body when valid credentials are supplied
**Feature:** qapass Basic-Auth API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the basic-auth protected endpoint authenticates the configured credentials and responds successfully with a JSON payload.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that response body contains "authenticated". → Expected: The response body contains the text "authenticated".
5. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 4 assertions pass — response status is 200; header "content-type" contains "application/json"; response body contains "authenticated"; response time is under 5000 ms.

**Tags:** api, smoke, auth

### TC-152 — Confirm GET /basic-auth/qauser/qapass returns authenticated=true and user=qauser in the response body
**Feature:** qapass Basic-Auth API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the documented httpbin basic-auth response fields: the authenticated flag is boolean true and the echoed user matches the credential username.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "authenticated" equals true. → Expected: The JSON field "authenticated" is present and equals true.
4. Assert that JSON "user" equals "qauser". → Expected: The JSON field "user" is present and equals "qauser".

**Expected Result:** All 3 assertions pass — response status is 200; JSON "authenticated" equals true; JSON "user" equals "qauser".

**Tags:** api, schema, auth

### TC-153 — Return 401 for GET /basic-auth/qauser/qapass when no credentials are supplied
**Feature:** qapass Basic-Auth API · **Priority:** P0 · **Type:** security · **Severity:** Critical

A deliberate no-auth request against the protected basic-auth endpoint must be rejected and not disclose the resource.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 401. → Expected: The response status code is exactly 401.

**Expected Result:** All 1 assertion pass — response status is 401.

**Tags:** api, auth, negative

### TC-154 — Return 401 for GET /basic-auth/qauser/wrongpass when the supplied password does not match the protected credential
**Feature:** qapass Basic-Auth API · **Priority:** P1 · **Type:** security · **Severity:** Major

Requesting the basic-auth endpoint whose expected password differs from the injected credential must fail authentication.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/wrongpass is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/wrongpass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 401. → Expected: The response status code is exactly 401.

**Expected Result:** All 1 assertion pass — response status is 401.

**Tags:** api, auth, negative

### TC-155 — Reject POST /basic-auth/qauser/qapass with a 4xx/405 because the endpoint only supports GET
**Feature:** qapass Basic-Auth API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sending a write method to the read-only basic-auth endpoint must be rejected rather than served or erroring 5xx.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://httpbin.org/basic-auth/qauser/qapass. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-156 — Confirm GET /basic-auth/qauser/qapass ignores an unknown query parameter and still returns 200
**Feature:** qapass Basic-Auth API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

An unrecognised query parameter must be safely ignored on the happy path rather than causing a server error.

**Precondition:** The API endpoint https://httpbin.org/basic-auth/qauser/qapass?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/basic-auth/qauser/qapass?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "application/json".

**Tags:** api, edge, resilience

### TC-157 — Verify GET /status/500 returns 200 as the configured expected status code
**Feature:** Status API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Exercises the documented happy path for the status endpoint and asserts the exact expected HTTP status code together with a response-time guard.

**Precondition:** The API endpoint https://httpbin.org/status/500 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/500. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, smoke, status

### TC-158 — Confirm GET /status/500 returns a content-type header on the happy path
**Feature:** Status API · **Priority:** P1 · **Type:** data · **Severity:** Major

Header-sanity guard verifying the status endpoint advertises a content-type on the expected successful response.

**Precondition:** The API endpoint https://httpbin.org/status/500 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/500. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text/html". → Expected: The "content-type" response header is present and contains "text/html".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text/html".

**Tags:** api, headers, status

### TC-159 — Reject GET /status/99999999 with a non-2xx status for an unknown status code id
**Feature:** Status API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests the status resource with an out-of-range numeric id that cannot map to a real HTTP status, expecting the API not to return a success response.

**Precondition:** The API endpoint https://httpbin.org/status/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, status

### TC-160 — Reject GET /status/abc with a 4xx when the status code id is non-numeric
**Feature:** Status API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a non-numeric id to the status endpoint where a numeric HTTP status code is required, expecting a client-error response rather than a 500.

**Precondition:** The API endpoint https://httpbin.org/status/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/status/abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, validation, status

### TC-161 — Verify GET /delay/2 returns 200 with a JSON content-type and a non-empty body
**Feature:** Delay API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the delayed endpoint responds successfully after the 2-second delay, returns JSON, and carries a populated response body.

**Precondition:** The API endpoint https://httpbin.org/delay/2 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "application/json". → Expected: The "content-type" response header is present and contains "application/json".
4. Assert that JSON "url" is present. → Expected: The JSON field "url" is present in the response body.

**Expected Result:** All 3 assertions pass — response status is 200; header "content-type" contains "application/json"; JSON "url" is present.

**Tags:** api, smoke, delay

### TC-162 — Confirm GET /delay/2 returns the request url as a string and echoes the headers object
**Feature:** Delay API · **Priority:** P1 · **Type:** data · **Severity:** Major

Validates the shape of the httpbin delay response: the url field is a string and the headers object is present, so downstream consumers can rely on the contract.

**Precondition:** The API endpoint https://httpbin.org/delay/2 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "url" is of type string. → Expected: The JSON field "url" is present and is of type string.
4. Assert that JSON "headers" is of type object. → Expected: The JSON field "headers" is present and is of type object.
5. Assert that JSON "args" is present. → Expected: The JSON field "args" is present in the response body.

**Expected Result:** All 4 assertions pass — response status is 200; JSON "url" is of type string; JSON "headers" is of type object; JSON "args" is present.

**Tags:** api, schema, delay

### TC-163 — Verify GET /delay/2 responds within 5000ms despite the built-in 2-second delay
**Feature:** Delay API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards the response-time budget so the intentional 2-second server delay stays within an acceptable ceiling and does not regress.

**Precondition:** The API endpoint https://httpbin.org/delay/2 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, delay

### TC-164 — Reject GET /delay/abc with a 404 when the delay segment is non-numeric
**Feature:** Delay API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The delay path segment requires a number; a non-numeric value must not be accepted and must fail cleanly rather than returning 200 or a 500.

**Precondition:** The API endpoint https://httpbin.org/delay/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, validation, delay

### TC-165 — Confirm GET /delay/2 ignores an unknown query parameter and still returns 200
**Feature:** Delay API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An unrecognised query parameter should be tolerated and echoed under args rather than causing a server error, so clients passing extra params are not broken.

**Precondition:** The API endpoint https://httpbin.org/delay/2?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://httpbin.org/delay/2?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that JSON "args.foo" equals "bar". → Expected: The JSON field "args.foo" is present and equals "bar".

**Expected Result:** All 2 assertions pass — response status is 200; JSON "args.foo" equals "bar".

**Tags:** api, negative, query, delay

### TC-166 — Verify the full posts lifecycle: create → read → update → verify → delete → confirm 404
**Feature:** posts flows · **Priority:** P0 · **Type:** e2e · **Severity:** Critical

Creates a post, reads it back by the returned id, updates it, verifies the update fields were persisted, deletes it, then confirms the same id is gone.

**Precondition:** The 6 endpoints in this flow are reachable and the environment allows creating and deleting posts records.

**Steps:**

1. Step 1 (create): send POST https://jsonplaceholder.typicode.com/posts with the step's JSON body. [data: {"title":"QA Automation Post","body":"Created from Postman collection","userId":1}] → Expected: The response status code is exactly 200. The JSON field "id" is present in the response body. The JSON field "title" is present and equals "QA Automation Post". The JSON field "userId" is present and equals 1. Captures "postId" from "id" for the following steps.
2. Step 2 (read): send GET https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is exactly 200. The JSON field "id" is present in the response body.
3. Step 3 (update): send PUT https://jsonplaceholder.typicode.com/posts/{{postId}} with the step's JSON body, filling {{…}} from earlier steps. [data: {"id":{{postId}},"title":"Updated Title","body":"Updated body","userId":1}] → Expected: The response status code is exactly 200. The JSON field "title" is present and equals "Updated Title". The JSON field "body" is present and equals "Updated body".
4. Step 4 (verify): send GET https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is exactly 200. The JSON field "id" is present in the response body.
5. Step 5 (delete): send DELETE https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is exactly 200.
6. Step 6 (confirm-gone): send GET https://jsonplaceholder.typicode.com/posts/{{postId}}, filling {{…}} from earlier steps. → Expected: The response status code is exactly 404.

**Expected Result:** All 13 assertions pass — create: response status is 200, JSON "id" is present, JSON "title" equals "QA Automation Post", JSON "userId" equals 1; read: response status is 200, JSON "id" is present; update: response status is 200, JSON "title" equals "Updated Title", JSON "body" equals "Updated body"; verify: response status is 200, JSON "id" is present; delete: response status is 200; confirm-gone: response status is 404.

**Tags:** api, flow, regression, posts
