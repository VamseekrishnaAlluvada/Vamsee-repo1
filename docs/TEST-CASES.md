# Test Cases

Generated by JBS IntelliQE — 93 test case(s).

| Module | Cases |
|---|---|
| API | 93 |

## API

### TC-001 — Verify GET / on postman-echo returns 200 with a content-type header
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy-path smoke test: the documented root request succeeds with the exact 200 status and carries a content-type header.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, smoke, root

### TC-002 — Confirm GET / on postman-echo returns a non-empty response body
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

The root request should return content, not an empty body, on success.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response body contains "postman". → Expected: The response body contains the text "postman".

**Expected Result:** All 2 assertions pass — response is successful (2xx); response body contains "postman".

**Tags:** api, schema, root

### TC-003 — Verify GET / on postman-echo responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Performance guard ensuring the root endpoint responds within an acceptable latency budget.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, root

### TC-004 — Return 404 for GET /99999999 when the resource id cannot exist
**Feature:** Root API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Requesting a non-existent numeric resource under the root should yield a 404 Not Found.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, not-found

### TC-005 — Return 404 for GET /nonexistent-resource-xyz on an unknown path segment
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A path that maps to no known handler should return a clean 404 rather than a server error.

**Precondition:** The API endpoint https://postman-echo.com/nonexistent-resource-xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/nonexistent-resource-xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, not-found

### TC-006 — Reject PUT / on postman-echo with a non-2xx status for an unsupported method
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

PUT is not a documented operation on the root resource; it must not succeed.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, method

### TC-007 — Reject PATCH / on postman-echo with a non-2xx status for an unsupported method
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

PATCH is not a documented operation on the root resource and should be rejected.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, method

### TC-008 — Verify GET /?debug=true is safely ignored and still returns 200
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unknown query parameter on the root should be ignored rather than causing an error.

**Precondition:** The API endpoint https://postman-echo.com/?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.

**Expected Result:** All 2 assertions pass — response status is 200; response is successful (2xx).

**Tags:** api, edge, query

### TC-009 — Verify GET / with multiple unknown query parameters still returns 200
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Several unrecognised query parameters together should not break the root response.

**Precondition:** The API endpoint https://postman-echo.com/?foo=1&bar=2&baz=three is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?foo=1&bar=2&baz=three. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge, query

### TC-010 — Verify GET / with a custom unknown request header returns 200 and is unaffected
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

An arbitrary custom header should be ignored and the root request should still succeed.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge, headers

### TC-011 — Verify GET / with an injection-shaped query value returns no 5xx server error
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A SQL/script-shaped query value must be handled cleanly without a server error (status below 500).

**Precondition:** The API endpoint https://postman-echo.com/?q=%27%20OR%20%271%27%3D%271 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?q=%27%20OR%20%271%27%3D%271. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, security, injection

### TC-012 — Confirm GET / on postman-echo exposes a content-type header on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Resilience check that the successful root response advertises its content type.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, headers, resilience

### TC-013 — Verify GET / on postman-echo returns 200 with a content-type header on the happy path
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented request to the root endpoint responds successfully and returns a content-type header.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text". → Expected: The "content-type" response header is present and contains "text".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text".

**Tags:** api, smoke

### TC-014 — Confirm GET / returns a non-empty response body on the happy path
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

Ensures the root endpoint returns a body payload rather than an empty response.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response body contains "postman". → Expected: The response body contains the text "postman".

**Expected Result:** All 2 assertions pass — response status is 200; response body contains "postman".

**Tags:** api, schema

### TC-015 — Verify GET / responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Guards the response-time budget for the root endpoint under normal conditions.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-016 — Confirm GET / with a trailing-path segment /index does not return a 500 server error
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Exercises path variance on the root resource and asserts the server responds without an internal error.

**Precondition:** The API endpoint https://postman-echo.com/index is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/index. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, edge

### TC-017 — Return a non-2xx status for GET /99999999 against a resource id that cannot exist
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests an out-of-range id under the root resource and asserts the endpoint does not treat it as success.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative

### TC-018 — Return a non-2xx status for GET /not-a-real-path with a malformed non-existent path
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a clearly invalid path segment and asserts the server rejects it rather than returning success.

**Precondition:** The API endpoint https://postman-echo.com/not-a-real-path is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/not-a-real-path. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative

### TC-019 — Confirm GET / ignores an unknown query parameter and still returns 200
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unrecognised query parameter to the root request and asserts it is safely ignored rather than causing an error.

**Precondition:** The API endpoint https://postman-echo.com/?unknownParam=abc123 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?unknownParam=abc123. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge

### TC-020 — Confirm DELETE / against the root URL does not return a 500 server error
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Issues a different HTTP method against the same root URL and asserts the server responds without an internal error.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, method

### TC-021 — Confirm GET / with Accept application/xml still returns a response below 500
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Requests an unsupported representation and asserts the root endpoint handles content negotiation without an internal error.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, content-negotiation

### TC-022 — Confirm GET / returns the same 200 status on two consecutive identical requests (idempotency)
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Repeats the documented root request to assert GET is idempotent and stable across calls.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, idempotency

### TC-023 — Confirm GET / with an injection-shaped query value returns a clean status below 500
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Sends a SQL/script-shaped query value against the root endpoint and asserts it is handled cleanly without a server error.

**Precondition:** The API endpoint https://postman-echo.com/?q=%27%20OR%201%3D1-- is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?q=%27%20OR%201%3D1--. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, security

### TC-024 — Verify HEAD / against the root URL returns a 2xx status with no body error
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Issues the sibling HEAD operation on the root resource and asserts it succeeds without a server error.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, method

### TC-025 — Verify GET https://postman-echo.com?foo=bar returns 200 with a content-type header on the happy path
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented request succeeds with the exact expected status code and advertises a content type.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text". → Expected: The "content-type" response header is present and contains "text".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text".

**Tags:** api, smoke, happy-path

### TC-026 — Confirm GET https://postman-echo.com?foo=bar returns a present, non-empty response body
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

No sample body was provided, so this asserts only that a 2xx response carries a non-empty payload rather than an empty document.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response body contains "<". → Expected: The response body contains the text "<".

**Expected Result:** All 2 assertions pass — response is successful (2xx); response body contains "<".

**Tags:** api, schema

### TC-027 — Ensure GET https://postman-echo.com?foo=bar responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Performance guard asserting the documented request returns under the agreed latency budget.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-028 — Return 404 for GET https://postman-echo.com/99999999 when the resource id cannot exist
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests a non-existent id on the root resource and expects a not-found response.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, not-found

### TC-029 — Reject GET https://postman-echo.com/not-a-number with a non-2xx status for a malformed id path
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Uses a non-numeric value where the resource id parameter is expected and asserts the request is not served as success.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-030 — Reject PUT https://postman-echo.com?foo=bar with a non-2xx status for an undocumented method on the root URL
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

PUT is not among the documented operations on the root resource; the endpoint must not treat it as a success.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, method

### TC-031 — Reject PATCH https://postman-echo.com?foo=bar with a non-2xx status for an undocumented method on the root URL
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

PATCH is not a documented operation on the root resource and should return a client/server error rather than a 2xx.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative, method

### TC-032 — Confirm GET https://postman-echo.com?foo=bar&unknownParam=xyz ignores an unknown query parameter and still returns 200
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Adds an unrecognised query parameter alongside the documented one and expects it to be safely ignored rather than causing a 500.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar&unknownParam=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar&unknownParam=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge, query

### TC-033 — Confirm GET https://postman-echo.com with no query parameters still returns 200
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Baseline request without the foo=bar parameter to verify the root endpoint does not depend on the sample query value.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge

### TC-034 — Ensure GET https://postman-echo.com?foo=%3Cscript%3E does not return a 500 for a script-shaped query value
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Resilience probe sending an encoded script payload in the query value and asserting the server responds with a client status, never a 5xx.

**Precondition:** The API endpoint https://postman-echo.com?foo=%3Cscript%3E is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=%3Cscript%3E. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, resilience, injection

### TC-035 — Verify HEAD https://postman-echo.com?foo=bar returns 200 for the documented sibling head operation
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Exercises the HEAD sibling on the root resource and expects a successful status with no assertion on the (absent) body.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge, method

### TC-036 — Verify GET https://postman-echo.com/99999999 returns 404 for a non-existent path
**Feature:** Root resource read · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy-path contract: requesting an undefined route on the root resource must respond with the documented 404 status.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, smoke, contract

### TC-037 — Confirm GET /99999999 404 response carries a content-type header
**Feature:** Root resource read · **Priority:** P1 · **Type:** data · **Severity:** Major

Resilience: the error response should still declare a content-type rather than an empty/malformed header set.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 404; header "content-type" contains "/".

**Tags:** api, headers

### TC-038 — Confirm GET /99999999 returns a non-2xx result and never a 5xx server error
**Feature:** Root resource read · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Negative guard: an unknown resource must be handled cleanly as a client-side error, not crash the server.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative

### TC-039 — Verify GET /99999999 404 response returns within 5000ms
**Feature:** Root resource read · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Performance guard on the documented request path.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 404; response time is under 5000 ms.

**Tags:** api, performance

### TC-040 — Verify GET /99999999 returns a non-empty body describing the 404 error
**Feature:** Root resource read · **Priority:** P2 · **Type:** data · **Severity:** Moderate

The error response should include a payload rather than an empty body, so clients can surface the failure.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response body contains "404". → Expected: The response body contains the text "404".

**Expected Result:** All 2 assertions pass — response status is 404; response body contains "404".

**Tags:** api, body

### TC-041 — Verify GET /99999999 with a non-numeric id segment returns 404
**Feature:** Root resource read · **Priority:** P1 · **Type:** negative · **Severity:** Major

Malformed path: a non-numeric id where the resource expects a numeric identifier must still resolve to a client error, not a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/not-a-real-id is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/not-a-real-id. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, path

### TC-042 — Confirm GET /99999999 ignores an unknown query parameter and still returns 404
**Feature:** Root resource read · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unrecognised query string must not change the outcome or trigger a server error.

**Precondition:** The API endpoint https://postman-echo.com/99999999?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response status is 404; response status is under 500.

**Tags:** api, query

### TC-043 — Verify GET /99999999/ with a trailing slash does not return a 5xx error
**Feature:** Root resource read · **Priority:** P2 · **Type:** edge · **Severity:** Minor

Path variance: a trailing slash on the unknown route should resolve to a clean client error, not crash the server.

**Precondition:** The API endpoint https://postman-echo.com/99999999/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, path, variance

### TC-044 — Confirm POST to /99999999 against the read URL is rejected with a client error
**Feature:** Root resource read · **Priority:** P1 · **Type:** negative · **Severity:** Major

Wrong HTTP method against the unknown route must return a 4xx (404/405), never a success or server error.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-045 — Confirm HEAD /99999999 mirrors the GET 404 without a body
**Feature:** Root resource read · **Priority:** P2 · **Type:** edge · **Severity:** Minor

Sibling HEAD operation on the same unknown route should return the same client-error status as GET.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, method, head

### TC-046 — Confirm GET /99999999 with Accept application/xml still returns 404 without a 5xx
**Feature:** Root resource read · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Content negotiation: an unsupported Accept type on the unknown route must not cause a server error.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, content-negotiation

### TC-047 — Confirm GET /99999999 returns 404 identically on two consecutive requests (idempotency)
**Feature:** Root resource read · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Idempotency: repeating the documented GET must yield the same 404 status with no state-dependent variation.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, idempotency

### TC-048 — Confirm GET /99999999 with a SQL-injection-shaped id returns a clean client error and does not echo the payload
**Feature:** Root resource read · **Priority:** P1 · **Type:** negative · **Severity:** Major

Injection probe: a SQL-shaped path segment must resolve to a clean 4xx without reflecting the raw payload or triggering a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/1%27%20OR%20%271%27%3D%271 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/1%27%20OR%20%271%27%3D%271. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, security, injection

### TC-049 — Verify GET /this-resource-does-not-exist returns exactly 404 for the documented request
**Feature:** this-resource-does-not-exist API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the endpoint responds with the expected 404 Not Found status for the documented request with no parameters.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, smoke, contract

### TC-050 — Confirm GET /this-resource-does-not-exist returns a non-empty response body with the 404
**Feature:** this-resource-does-not-exist API · **Priority:** P1 · **Type:** data · **Severity:** Major

Verifies the 404 response carries a body payload rather than an empty response.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that header "content-type" is present. → Expected: The "content-type" response header is present on the response.

**Expected Result:** All 2 assertions pass — response status is 404; header "content-type" is present.

**Tags:** api, schema

### TC-051 — Confirm GET /this-resource-does-not-exist responds within 5000ms on the happy path
**Feature:** this-resource-does-not-exist API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards response time for the documented 404 request to catch latency regressions.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 404; response time is under 5000 ms.

**Tags:** api, performance

### TC-052 — Confirm GET /this-resource-does-not-exist sets a content-type header on the 404 response
**Feature:** this-resource-does-not-exist API · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Verifies the server labels the 404 response body with a content-type header.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that header "content-type" is present. → Expected: The "content-type" response header is present on the response.

**Expected Result:** All 2 assertions pass — response status is 404; header "content-type" is present.

**Tags:** api, headers

### TC-053 — Return 404 for GET /this-resource-does-not-exist/99999999 when a non-existent sub-resource id is requested
**Feature:** this-resource-does-not-exist API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms requesting a deeply non-existent id under the missing resource still yields a 404 and never a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative

### TC-054 — Return 404 for GET /this-resource-does-not-exist/abc when a non-numeric id segment is supplied
**Feature:** this-resource-does-not-exist API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms a malformed non-numeric path segment on the missing resource returns a client 4xx rather than a server error.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist/abc. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-055 — Return a documented 4xx for POST /this-resource-does-not-exist when using an unsupported HTTP method
**Feature:** this-resource-does-not-exist API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Verifies that a write method against the missing list resource is rejected with a client 4xx (404/405) and never a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 404, 405; response is an error (non-2xx).

**Tags:** api, negative, methods

### TC-056 — Confirm GET /this-resource-does-not-exist?foo=bar still returns 404 when an unknown query parameter is supplied
**Feature:** this-resource-does-not-exist API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Verifies an unrecognised query parameter is ignored and does not change the 404 outcome or cause a 500.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, query

### TC-057 — Confirm GET /this-resource-does-not-exist/ with a trailing slash does not return a 5xx
**Feature:** this-resource-does-not-exist API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Verifies trailing-slash path variance on the missing resource is handled cleanly with a 4xx rather than a server error.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, edge, path

### TC-058 — Confirm GET /This-Resource-Does-Not-Exist with altered path casing does not return a 5xx
**Feature:** this-resource-does-not-exist API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Verifies case variance on the resource path is handled with a client 4xx rather than a server error.

**Precondition:** The API endpoint https://postman-echo.com/This-Resource-Does-Not-Exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/This-Resource-Does-Not-Exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, edge, path

### TC-059 — Confirm GET /this-resource-does-not-exist with Accept application/xml still returns 404 without a 5xx
**Feature:** this-resource-does-not-exist API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Verifies content negotiation for an unsupported media type does not alter the 404 outcome or trigger a server error.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 404, 406. → Expected: The response status code is one of 404, 406.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 404, 406; response is an error (non-2xx).

**Tags:** api, edge, content-negotiation

### TC-060 — Confirm GET /this-resource-does-not-exist does not reflect an injected script query value in the 404 body
**Feature:** this-resource-does-not-exist API · **Priority:** P2 · **Type:** negative · **Severity:** Major

Sends a script-shaped query value and asserts a clean 4xx with the raw payload not echoed back in the response.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist?q=%3Cscript%3Ealert(1)%3C%2Fscript%3E is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist?q=%3Cscript%3Ealert(1)%3C%2Fscript%3E. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, security, injection

### TC-061 — Confirm GET /this-resource-does-not-exist returns the same 404 status on two consecutive identical requests
**Feature:** this-resource-does-not-exist API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Verifies the endpoint is idempotent by asserting a repeated documented request yields the same 404 status.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, idempotency

### TC-062 — Verify POST / returns 405 Method Not Allowed on the root resource
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path contract: POST is not permitted on the root resource and the server responds with exactly 405.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that header "content-type" contains "html". → Expected: The "content-type" response header is present and contains "html".

**Expected Result:** All 2 assertions pass — response status is 405; header "content-type" contains "html".

**Tags:** api, smoke, contract

### TC-063 — Confirm the 405 response for POST / carries a content-type header and a non-empty body
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

Ensures the rejection response is well-formed with a content-type header and an explanatory body rather than an empty payload.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that header "content-type" contains "html". → Expected: The "content-type" response header is present and contains "html".
4. Assert that response body contains "". → Expected: The response body contains the text "".

**Expected Result:** All 3 assertions pass — response status is 405; header "content-type" contains "html"; response body contains "".

**Tags:** api, schema

### TC-064 — Verify POST / responds within 5000ms while returning 405
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Performance guard on the happy path to catch latency regressions in the method-rejection path.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 405; response time is under 5000 ms.

**Tags:** api, performance

### TC-065 — Reject POST /99999999 for a non-existent id with a 4xx (404 or 405)
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Posting to an id that cannot exist must yield a clean client error, not a server error.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 404, 405; response is an error (non-2xx).

**Tags:** api, negative

### TC-066 — Reject POST /not-a-number for a non-numeric id path with a 4xx
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A malformed, non-numeric path segment where an id is expected must be handled with a client error and never a 500.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative

### TC-067 — Reject PUT / on the root resource with a 4xx as an unsupported method
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Confirms another write method against the same URL is also refused with a client error rather than being silently accepted.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, methods

### TC-068 — Reject PATCH / on the root resource with a 4xx as an unsupported method
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Confirms PATCH against the root URL is refused with a client error and does not reach a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, methods

### TC-069 — Confirm POST /?debug=true ignores an unknown query parameter and still returns 405
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An unrecognised query parameter must not change the method-rejection behaviour or cause a 500.

**Precondition:** The API endpoint https://postman-echo.com/?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 405; response is an error (non-2xx).

**Tags:** api, negative, query

### TC-070 — Confirm POST / with an empty JSON body {} is still rejected with 405
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

The method is refused before body parsing, so an empty JSON payload must not alter the 405 outcome.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/ carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 405; response is an error (non-2xx).

**Tags:** api, negative, payload

### TC-071 — Confirm POST / with a malformed JSON body is rejected with a 4xx
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

A syntactically broken payload against a disallowed method must produce a client error and never a 500.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/ carrying the JSON request body shown above. [data: { "broken": ] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, payload

### TC-072 — Confirm POST / with a SQL-injection-shaped query value returns a clean 405 without echoing the payload
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Major

An injection-shaped query value must be handled with a client-side rejection and the raw payload must not be reflected back in the body.

**Precondition:** The API endpoint https://postman-echo.com/?q=1%27%20OR%20%271%27%3D%271 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/?q=1%27%20OR%20%271%27%3D%271. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, security

### TC-073 — Verify POST / returns the same 405 status on two consecutive calls
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Consistency check that the method rejection is stable and deterministic across repeated requests.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, resilience, idempotency

### TC-074 — Verify DELETE / returns 405 Method Not Allowed on the root resource
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

The root resource does not expose a delete operation; the documented happy-path response for DELETE / is HTTP 405.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 405; response time is under 5000 ms.

**Tags:** api, smoke, contract

### TC-075 — Confirm DELETE / responds with a content-type header on the 405 response
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

Header sanity on the happy path — the 405 response should still carry a content-type so clients can parse the error body.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that header "content-type" contains "text". → Expected: The "content-type" response header is present and contains "text".

**Expected Result:** All 2 assertions pass — response status is 405; header "content-type" contains "text".

**Tags:** api, headers

### TC-076 — Verify DELETE /99999999 for a non-existent id returns a non-2xx status
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Deleting an id that cannot exist must not succeed; the method is unsupported so a 4xx (405/404) is expected, never a 2xx.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, negative

### TC-077 — Verify DELETE /not-a-number with a non-numeric id returns a non-2xx status
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

The id parameter is numeric; a malformed alphabetic path segment must be rejected with a 4xx rather than succeeding or 500-ing.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-078 — Verify DELETE /?debug=true ignores an unknown query parameter and still returns 405
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unrecognised query parameter must not change behaviour or cause a 500; the response should remain the documented 405.

**Precondition:** The API endpoint https://postman-echo.com/?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, edge

### TC-079 — Verify DELETE / with a request body is rejected with 405 and the body is not honoured
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sending a JSON payload with an unsupported method must not enable the operation; the response stays 405.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/ carrying the JSON request body shown above. [data: {"id":1}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, negative

### TC-080 — Confirm GET / on the same root resource returns 200 to prove only the delete verb is blocked
**Feature:** Root API · **Priority:** P1 · **Type:** api · **Severity:** Major

A sibling read operation on the identical URL should succeed with 200, isolating the 405 to the DELETE verb rather than the path itself.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, method

### TC-081 — Verify DELETE / with Accept application/xml still returns 405 without a 500
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Content negotiation must not affect the method-not-allowed outcome; an unsupported Accept type should not error the server.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response status is 405; response status is under 500.

**Tags:** api, content-negotiation

### TC-082 — Verify DELETE / issued twice is idempotent and returns 405 on both attempts
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Repeating the unsupported request must not change state or vary the outcome; the second call returns the same 405.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, idempotency

### TC-083 — Verify DELETE /?id=1%27%20OR%20%271%27%3D%271 returns a clean 4xx and does not echo the injection payload
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Major

A SQL-injection-shaped query value must produce a clean method-not-allowed/4xx response and must not reflect the raw payload back to the client.

**Precondition:** The API endpoint https://postman-echo.com/?id=1%27%20OR%20%271%27%3D%271 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/?id=1%27%20OR%20%271%27%3D%271. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, security, injection

### TC-084 — Verify HEAD / returns 200 with response headers and no body
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the root endpoint responds successfully to a HEAD request and returns header metadata as expected.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text/html". → Expected: The "content-type" response header is present and contains "text/html".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text/html".

**Tags:** api, smoke, head

### TC-085 — Confirm HEAD / responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards the root HEAD request against latency regressions.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, head

### TC-086 — Confirm HEAD / advertises a text/html content-type header
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

Header sanity check asserting the documented content-type is present on the successful HEAD response.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text/html". → Expected: The "content-type" response header is present and contains "text/html".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text/html".

**Tags:** api, headers, head

### TC-087 — Return 404 for HEAD /99999999 when the resource id cannot exist
**Feature:** Root API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Requests a non-existent numeric id under the root resource and expects a not-found response.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, head

### TC-088 — Reject HEAD /not-a-number with a 4xx for a non-numeric resource id
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a malformed, non-numeric path segment where a numeric id is expected and asserts a client error.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation, head

### TC-089 — Reject PATCH / against the root URL as an unsupported HTTP method
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Sends an unsupported method to the same root URL and expects a non-2xx client error rather than success.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PATCH request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-090 — Confirm HEAD /?foo=bar ignores an unknown query parameter and returns 200
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Verifies that an unexpected query parameter does not trigger a server error and the root still responds successfully.

**Precondition:** The API endpoint https://postman-echo.com/?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "text/html". → Expected: The "content-type" response header is present and contains "text/html".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "text/html".

**Tags:** api, edge, query, head

### TC-091 — Confirm HEAD / with Accept: application/xml still returns 200
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Content negotiation check asserting that an unsupported Accept header is tolerated and the root returns a successful HEAD response.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge, negotiation, head

### TC-092 — Confirm HEAD with trailing-slash root path does not return 5xx
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Exercises the root path with an explicit trailing slash and asserts the server responds without an internal error.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, edge, path, head

### TC-093 — Verify root lifecycle: POST base rejected (405) then GET missing id returns 404
**Feature:** root flows · **Priority:** P1 · **Type:** e2e · **Severity:** Major

Attempts to create a root by POSTing to the base URL and confirms the method is not allowed (405), then reads a non-existent root id and confirms it is not found (404). Documents the negative lifecycle for the root resource on postman-echo.

**Precondition:** The 2 endpoints in this flow are reachable and the environment allows creating and deleting root records.

**Steps:**

1. Step 1 (create): send POST https://postman-echo.com. → Expected: The response status code is exactly 405.
2. Step 2 (read): send GET https://postman-echo.com/99999999. → Expected: The response status code is exactly 404.

**Expected Result:** All 2 assertions pass — create: response status is 405; read: response status is 404.

**Tags:** api, flow, regression
