# Test Cases

Generated by JBS IntelliQE — 5 test case(s).

| Module | Cases |
|---|---|
| API | 5 |

## API

### TC-001 — Verify GET /posts returns 200 with a non-empty JSON array of posts
**Feature:** Posts API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented request succeeds and returns the posts collection as a JSON array.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts → Expected: The API returns a response
2. Assert: response status is 200 → Expected: Assertion holds
3. Assert: header "content-type" contains "application/json" → Expected: Assertion holds
4. Assert: JSON body is a non-empty array → Expected: Assertion holds
5. Assert: response time is under 5000 ms → Expected: Assertion holds

**Expected Result:** response status is 200; header "content-type" contains "application/json"; JSON body is a non-empty array; response time is under 5000 ms

**Tags:** api, smoke

### TC-002 — Verify each post in GET /posts carries the documented schema (userId, id, title, body)
**Feature:** Posts API · **Priority:** P0 · **Type:** data · **Severity:** Major

Asserts existence and types of every key field from the sample body on the first element of the collection.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts → Expected: The API returns a response
2. Assert: response status is 200 → Expected: Assertion holds
3. Assert: JSON "0.userId" is present → Expected: Assertion holds
4. Assert: JSON "0.userId" is of type number → Expected: Assertion holds
5. Assert: JSON "0.id" is of type number → Expected: Assertion holds
6. Assert: JSON "0.id" equals 1 → Expected: Assertion holds
7. Assert: JSON "0.title" is of type string → Expected: Assertion holds
8. Assert: JSON "0.body" is of type string → Expected: Assertion holds

**Expected Result:** response status is 200; JSON "0.userId" is present; JSON "0.userId" is of type number; JSON "0.id" is of type number; JSON "0.id" equals 1; JSON "0.title" is of type string; JSON "0.body" is of type string

**Tags:** api, schema

### TC-003 — Reject GET /posts/99999999 for a non-existent post id (404)
**Feature:** Posts API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A well-formed but unknown resource id must return Not Found rather than a post payload.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/99999999 → Expected: The API returns a response
2. Assert: response status is 404 → Expected: Assertion holds
3. Assert: response is an error (non-2xx) → Expected: Assertion holds

**Expected Result:** response status is 404; response is an error (non-2xx)

**Tags:** api, negative

### TC-004 — Handle GET /posts/abc with a non-numeric id without a server error (4xx)
**Feature:** Posts API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

A malformed, non-numeric path segment where a numeric id is expected must fail cleanly rather than return 5xx.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts/abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts/abc → Expected: The API returns a response
2. Assert: response status is one of 400, 404 → Expected: Assertion holds
3. Assert: response is an error (non-2xx) → Expected: Assertion holds

**Expected Result:** response status is one of 400, 404; response is an error (non-2xx)

**Tags:** api, negative

### TC-005 — Confirm GET /posts?userId=1 filters and still returns 200 with a non-empty array
**Feature:** Posts API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

A supported query filter should return a successful, non-empty JSON array scoped to the requested user.

**Precondition:** The API endpoint https://jsonplaceholder.typicode.com/posts?userId=1 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://jsonplaceholder.typicode.com/posts?userId=1 → Expected: The API returns a response
2. Assert: response status is 200 → Expected: Assertion holds
3. Assert: JSON body is a non-empty array → Expected: Assertion holds
4. Assert: JSON "0.userId" equals 1 → Expected: Assertion holds

**Expected Result:** response status is 200; JSON body is a non-empty array; JSON "0.userId" equals 1

**Tags:** api, query
