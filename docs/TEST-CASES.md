# Test Cases

Generated by JBS IntelliQE — 90 test case(s).

| Module | Cases |
|---|---|
| API | 90 |

## API

### TC-001 — Verify GET https://postman-echo.com/ returns 200 on the happy path
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the root endpoint responds successfully with a body and a content-type header.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, smoke

### TC-002 — Verify GET https://postman-echo.com/ responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P1 · **Type:** api · **Severity:** Major

Guards the response time of the documented root request.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-007 — Return a 4xx for GET https://postman-echo.com/not-a-real-id with a non-numeric resource id
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A malformed, non-numeric id on the root resource should be rejected, not served.

**Precondition:** The API endpoint https://postman-echo.com/not-a-real-id is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/not-a-real-id. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 1 assertion pass — response is an error (non-2xx).

**Tags:** api, validation

### TC-008 — Confirm DELETE https://postman-echo.com/ does not return a 5xx when using an unexpected method on the root
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Exercising a sibling write method against the root URL should be handled cleanly.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, method

### TC-009 — Confirm GET https://postman-echo.com (no trailing slash) does not return a 5xx
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Path variance without the trailing slash should behave consistently and not error.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, path-variance

### TC-010 — Confirm GET https://postman-echo.com/ returns the same status when called twice (idempotency)
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

A safe GET on the root should be idempotent and return a consistent successful status.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, idempotency

### TC-011 — Confirm GET https://postman-echo.com/?q=<script>alert(1)</script> returns a clean status without a 5xx
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Major

A script-shaped query value should be handled safely and not trigger a server error.

**Precondition:** The API endpoint https://postman-echo.com/?q=%3Cscript%3Ealert(1)%3C%2Fscript%3E is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?q=%3Cscript%3Ealert(1)%3C%2Fscript%3E. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, security, injection

### TC-012 — Confirm HEAD https://postman-echo.com/ returns a 2xx with no server error
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

The sibling HEAD operation on the root should succeed and expose response metadata.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, head

### TC-013 — Verify GET / on postman-echo returns 200 with a non-empty response body
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented root request succeeds and returns a payload from the service.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, smoke, root

### TC-014 — Verify GET / responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Guards the root endpoint against latency regressions on a successful request.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-015 — Confirm GET / returns a Content-Type header on the successful response
**Feature:** Root API · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Header sanity check ensuring the root response advertises a media type.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response is successful (2xx); header "content-type" contains "/".

**Tags:** api, headers

### TC-016 — Return 404 for GET /99999999 when the requested resource id does not exist
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Uses a plausible non-existent numeric id to confirm the service reports not-found rather than erroring.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, not-found

### TC-017 — Return 404 for GET /nonexistent-resource-xyz when a non-numeric unknown path is requested
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms an unmapped, non-numeric path is rejected as not-found instead of returning 5xx.

**Precondition:** The API endpoint https://postman-echo.com/nonexistent-resource-xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/nonexistent-resource-xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 404. → Expected: The response status code is one of 400, 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 404; response is an error (non-2xx).

**Tags:** api, negative, malformed-path

### TC-018 — Reject PUT / with a 4xx/405 since PUT is not a supported method on the root resource
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Exercises an undocumented HTTP method against the same root URL and expects a client error, not success.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-019 — Confirm GET /?debug=true ignores an unknown query parameter and still returns 200
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Verifies an unrecognised query string does not break the root endpoint or trigger a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.

**Expected Result:** All 2 assertions pass — response status is 200; response is successful (2xx).

**Tags:** api, query, robustness

### TC-020 — Confirm GET / with multiple unknown query parameters still returns 200 without a server error
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Sends several unrecognised query parameters to ensure the root endpoint remains resilient.

**Precondition:** The API endpoint https://postman-echo.com/?foo=1&bar=baz&x=y is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/?foo=1&bar=baz&x=y. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is successful (2xx); response status is under 500.

**Tags:** api, query, robustness

### TC-021 — Confirm GET / with Accept: application/xml still returns 200 rather than a 5xx
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Content-negotiation probe ensuring an unsupported Accept type is handled gracefully by the root endpoint.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, content-negotiation

### TC-022 — Verify HEAD / returns 200 with headers and no body on the root resource
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Confirms the documented HEAD sibling operation on the root URL succeeds and exposes response headers.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, head

### TC-023 — Confirm two consecutive GET / requests return the same 200 status (idempotency)
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Repeats the documented root request to verify a stable, idempotent successful response.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.

**Expected Result:** All 2 assertions pass — response status is 200; response is successful (2xx).

**Tags:** api, idempotency

### TC-024 — Verify GET https://postman-echo.com?foo=bar returns 200 for the documented request
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Happy-path smoke check confirming the root endpoint responds successfully with the documented query parameter.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, smoke, positive

### TC-025 — Confirm GET https://postman-echo.com?foo=bar returns a present, non-empty response body
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

Ensures the successful response actually carries a payload rather than an empty body, without asserting undocumented field names.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response body contains "". → Expected: The response body contains the text "".

**Expected Result:** All 2 assertions pass — response status is 200; response body contains "".

**Tags:** api, schema

### TC-026 — Verify GET https://postman-echo.com?foo=bar responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Performance guard ensuring the root endpoint responds within an acceptable latency budget.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance

### TC-027 — Confirm content-type header is returned on the GET https://postman-echo.com?foo=bar happy path
**Feature:** Root API · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Resilience header-sanity check verifying the successful response advertises a content-type.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, headers

### TC-028 — Return 404 for GET https://postman-echo.com/99999999 when the resource id does not exist
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Negative case requesting a non-existent numeric id under the root resource; the server must not treat it as a valid resource.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, negative, not-found

### TC-029 — Return a 4xx for GET https://postman-echo.com/not-a-number when a non-numeric id is supplied
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Negative case supplying a malformed, non-numeric path segment where a numeric id is expected; the endpoint must reject or not-find it rather than error.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-030 — Reject PUT https://postman-echo.com?foo=bar with a 4xx as the method is not supported on the root resource
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Negative case sending an unsupported HTTP method against the root URL; the resource only exposes GET/POST/DELETE/HEAD, so PUT must be refused.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP PUT request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-031 — Confirm GET https://postman-echo.com?foo=bar&unknownParam=xyz ignores an unknown query parameter and still returns 200
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Resilience case adding an unrecognised query parameter; the endpoint must ignore it and respond successfully rather than error.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar&unknownParam=xyz is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar&unknownParam=xyz. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge, query

### TC-032 — Confirm GET https://postman-echo.com?foo= handles an empty query value without a 500 error
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Edge case supplying the documented query parameter with an empty value; the endpoint must respond cleanly instead of erroring.

**Precondition:** The API endpoint https://postman-echo.com?foo= is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, edge, query

### TC-033 — Verify HEAD https://postman-echo.com?foo=bar returns 200 with headers and no body
**Feature:** Root API · **Priority:** P2 · **Type:** api · **Severity:** Moderate

Confirms the sibling HEAD operation on the root resource responds successfully, exercising the same URL with a body-less method.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, head

### TC-034 — Confirm repeating GET https://postman-echo.com?foo=bar twice returns the same 200 status (idempotency)
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Resilience check asserting the read is idempotent: the documented request yields a stable successful status on repeat.

**Precondition:** The API endpoint https://postman-echo.com?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, idempotency

### TC-035 — Verify GET /99999999 returns exactly 404 for a non-existent resource
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Documented happy path for this endpoint: requesting an id that does not exist must resolve to a 404 Not Found.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that header "content-type" contains "html". → Expected: The "content-type" response header is present and contains "html".

**Expected Result:** All 2 assertions pass — response status is 404; header "content-type" contains "html".

**Tags:** api, smoke, not-found

### TC-036 — Confirm GET /99999999 returns a non-empty body describing the missing resource
**Feature:** Root API · **Priority:** P1 · **Type:** data · **Severity:** Major

With no sample body documented, assert only that the 404 response carries a non-empty payload rather than an empty response.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response body contains "Cannot GET". → Expected: The response body contains the text "Cannot GET".

**Expected Result:** All 2 assertions pass — response status is 404; response body contains "Cannot GET".

**Tags:** api, schema, not-found

### TC-037 — Verify GET /99999999 responds within 5000ms even for a 404
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Response-time guard on the happy path to catch latency regressions on the not-found route.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 404; response time is under 5000 ms.

**Tags:** api, performance

### TC-038 — Reject GET /not-a-number with 404 when the id segment is non-numeric
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A malformed, non-numeric path segment must still resolve to a client 404, never a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, validation

### TC-039 — Reject GET /99999999 with a 4xx client error and never a server 5xx
**Feature:** Root API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Confirms the not-found route stays firmly in the 4xx band and does not degrade into a server error.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative

### TC-040 — Return a non-2xx status for POST /99999999 using the wrong HTTP method
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

The read endpoint must not accept a POST on the same URL; expect a 404/405-class rejection, not success.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, method

### TC-041 — Confirm GET /99999999?debug=true ignores an unknown query parameter and still returns 404
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unrecognised query parameter must be tolerated and yield the same 404, never a 500.

**Precondition:** The API endpoint https://postman-echo.com/99999999?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response status is 404; response status is under 500.

**Tags:** api, query, edge

### TC-042 — Confirm GET /99999999/ with a trailing slash does not trigger a 500
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Trailing-slash variance on the path must resolve to a client 4xx and never a server error.

**Precondition:** The API endpoint https://postman-echo.com/99999999/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, edge, path

### TC-043 — Verify GET /99999999 with Accept application/xml still returns a 404
**Feature:** Root API · **Priority:** P3 · **Type:** negative · **Severity:** Minor

Requesting an unsupported representation must not change the not-found outcome nor cause a server error.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, content-negotiation

### TC-044 — Confirm GET /99999999 sets a content-type header on the 404 response
**Feature:** Root API · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Header sanity check: the not-found response must declare a content-type for its error body.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that header "content-type" contains "charset". → Expected: The "content-type" response header is present and contains "charset".

**Expected Result:** All 2 assertions pass — response status is 404; header "content-type" contains "charset".

**Tags:** api, headers

### TC-045 — Verify GET /99999999 is idempotent by returning 404 on two consecutive calls
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Repeating the documented request must yield the same 404 status, proving the read is side-effect free.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.

**Expected Result:** All 1 assertion pass — response status is 404.

**Tags:** api, idempotency

### TC-046 — Return a clean 4xx for GET /99999999 with a SQL-injection-shaped query value without echoing the payload
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Injection probe: a script/SQL-shaped query value must produce a safe client error and must not reflect the raw payload back in the body.

**Precondition:** The API endpoint https://postman-echo.com/99999999?q=%27%20OR%201%3D1%20-- is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/99999999?q=%27%20OR%201%3D1%20--. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, security, injection

### TC-047 — Verify GET /this-resource-does-not-exist returns exactly 404 for the documented request
**Feature:** This-resource-does-not-exist API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the endpoint responds with the expected 404 status for a non-existent resource path.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, smoke, not-found

### TC-048 — Confirm GET /this-resource-does-not-exist returns a non-empty response body describing the 404
**Feature:** This-resource-does-not-exist API · **Priority:** P1 · **Type:** data · **Severity:** Major

The 404 response should carry a body payload rather than an empty response.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response body contains "Cannot GET". → Expected: The response body contains the text "Cannot GET".

**Expected Result:** All 2 assertions pass — response status is 404; response body contains "Cannot GET".

**Tags:** api, schema, not-found

### TC-049 — Confirm GET /this-resource-does-not-exist responds within 5000ms even for a 404
**Feature:** This-resource-does-not-exist API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards that the not-found path returns promptly and does not hang.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 404; response time is under 5000 ms.

**Tags:** api, performance, not-found

### TC-050 — Confirm GET /this-resource-does-not-exist returns a content-type header on the 404 response
**Feature:** This-resource-does-not-exist API · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Verifies the server labels the 404 body with a content-type header.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 404; header "content-type" contains "/".

**Tags:** api, headers, not-found

### TC-051 — Verify GET /this-resource-does-not-exist/99999999 for a deep unknown sub-path still returns 404
**Feature:** This-resource-does-not-exist API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A deeper non-existent path under the same missing resource should also resolve to 404, not 500.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, not-found

### TC-052 — Verify GET /this-resource-does-not-exist with a non-numeric segment returns 404 without a server error
**Feature:** This-resource-does-not-exist API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A malformed trailing segment must still yield a client 4xx, never a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, not-found

### TC-053 — Verify POST to /this-resource-does-not-exist is rejected with a 4xx rather than succeeding
**Feature:** This-resource-does-not-exist API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A write method against the non-existent read-only resource must not return a 2xx.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/this-resource-does-not-exist carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, method

### TC-054 — Verify DELETE against /this-resource-does-not-exist does not return a 2xx
**Feature:** This-resource-does-not-exist API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An unsupported DELETE on the missing resource must be refused, not silently accepted.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, method

### TC-055 — Verify GET /this-resource-does-not-exist with an unknown query parameter still returns 404
**Feature:** This-resource-does-not-exist API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An unexpected query string should be ignored and the path should still resolve to 404 rather than 500.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist?foo=bar&limit=10 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist?foo=bar&limit=10. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, negative, query

### TC-056 — Verify GET /this-resource-does-not-exist/ with a trailing slash does not trigger a 500
**Feature:** This-resource-does-not-exist API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Trailing-slash variance on the missing path must remain a client 4xx.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist/ is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, edge, not-found

### TC-057 — Verify GET /THIS-RESOURCE-DOES-NOT-EXIST with an uppercased path does not return a 5xx
**Feature:** This-resource-does-not-exist API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Case variance on the path segment should still yield a clean 4xx rather than a server error.

**Precondition:** The API endpoint https://postman-echo.com/THIS-RESOURCE-DOES-NOT-EXIST is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/THIS-RESOURCE-DOES-NOT-EXIST. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, edge, case

### TC-058 — Verify GET /this-resource-does-not-exist ignores an Accept: application/xml header and still returns 404
**Feature:** This-resource-does-not-exist API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Content negotiation for an unsupported type should not change the not-found outcome or cause a 5xx.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, edge, content-negotiation

### TC-059 — Verify GET /this-resource-does-not-exist returns a stable 404 on a repeated identical request
**Feature:** This-resource-does-not-exist API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms the not-found response is idempotent and deterministic across two identical GET calls.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 404. → Expected: The response status code is exactly 404.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 404; response is an error (non-2xx).

**Tags:** api, idempotency, not-found

### TC-060 — Verify GET /this-resource-does-not-exist with a SQL-injection-shaped path segment returns a clean 4xx and does not echo the payload
**Feature:** This-resource-does-not-exist API · **Priority:** P1 · **Type:** negative · **Severity:** Major

An injection probe in the path must yield a client error and must not reflect the raw malicious string back in the body.

**Precondition:** The API endpoint https://postman-echo.com/this-resource-does-not-exist/1%27%20OR%20%271%27%3D%271 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com/this-resource-does-not-exist/1%27%20OR%20%271%27%3D%271. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, security, injection

### TC-061 — Verify POST https://postman-echo.com returns 405 Method Not Allowed on the root resource
**Feature:** Root Resource API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented happy-path contract: POST against the root resource is not allowed and responds with exactly 405.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that header "content-type" contains "text". → Expected: The "content-type" response header is present and contains "text".

**Expected Result:** All 2 assertions pass — response status is 405; header "content-type" contains "text".

**Tags:** api, smoke, contract

### TC-062 — Confirm POST https://postman-echo.com responds under 5000ms on the 405 happy path
**Feature:** Root Resource API · **Priority:** P1 · **Type:** edge · **Severity:** Moderate

Response-time guard on the documented POST-to-root request to catch latency regressions.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 405; response time is under 5000 ms.

**Tags:** api, performance

### TC-063 — Verify POST https://postman-echo.com returns a non-2xx status confirming the write is rejected
**Feature:** Root Resource API · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Asserts the negative outcome directly: POST to root must not succeed as a create.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is 405.

**Tags:** api, negative

### TC-064 — Reject POST https://postman-echo.com with 405 even when a JSON body is supplied
**Feature:** Root Resource API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms a well-formed JSON payload does not change the method-not-allowed outcome on the root resource.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com carrying the JSON request body shown above. [data: {"name":"probe"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, negative, payload

### TC-065 — Reject POST https://postman-echo.com with a 4xx when the body is malformed JSON
**Feature:** Root Resource API · **Priority:** P1 · **Type:** negative · **Severity:** Major

A syntactically broken payload must still be refused; POST to root is not allowed regardless of body validity.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com carrying the JSON request body shown above. [data: {"name":] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 400, 405. → Expected: The response status code is one of 400, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 400, 405; response is an error (non-2xx).

**Tags:** api, negative, payload

### TC-066 — Reject POST https://postman-echo.com with 405 when an empty JSON object body is sent
**Feature:** Root Resource API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

An empty object payload must not be treated as a valid create on the root resource.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com carrying the JSON request body shown above. [data: {}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, negative, payload

### TC-067 — Confirm POST https://postman-echo.com does not 500 when an unknown query parameter is added
**Feature:** Root Resource API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unexpected query string must be ignored and still yield the method-not-allowed response, never a server error.

**Precondition:** The API endpoint https://postman-echo.com?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 2 assertions pass — response status is under 500; response status is 405.

**Tags:** api, edge, query

### TC-068 — Confirm POST https://postman-echo.com/ with a trailing slash does not 500 and stays a 4xx
**Feature:** Root Resource API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Path variance on the root resource must be handled gracefully without a server error.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, edge, path

### TC-069 — Return 404 for POST https://postman-echo.com/99999999 against a non-existent id
**Feature:** Root Resource API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Posting to a resource id that cannot exist should surface a not-found rather than a create; grounded in the sibling read GET /99999999.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is one of 404, 405; response is an error (non-2xx).

**Tags:** api, negative, not-found

### TC-070 — Confirm POST https://postman-echo.com/not-a-number does not 500 on a non-numeric id
**Feature:** Root Resource API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

A malformed, non-numeric id segment must produce a clean 4xx rather than a server error.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is under 500; response is an error (non-2xx).

**Tags:** api, negative, path

### TC-071 — Verify POST https://postman-echo.com returns a clean 4xx and does not echo a script-shaped body value
**Feature:** Root Resource API · **Priority:** P2 · **Type:** negative · **Severity:** Major

Injection probe: a script-shaped payload must be refused and the raw payload must not appear in the response.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com carrying the JSON request body shown above. [data: {"name":"<script>alert(1)</script>"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 405; response is an error (non-2xx).

**Tags:** api, security, injection

### TC-072 — Confirm POST https://postman-echo.com returns the same 405 status on two identical requests
**Feature:** Root Resource API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Repeating the disallowed POST must consistently return 405, confirming a stable rejection with no side effects.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, consistency

### TC-073 — Verify DELETE / on postman-echo returns 405 Method Not Allowed within 5s
**Feature:** Root Resource API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Documented happy path: the DELETE verb is not permitted on the root resource, so the service must reject it with the exact 405 status and respond promptly.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 405; response time is under 5000 ms.

**Tags:** api, smoke, contract

### TC-074 — Confirm the 405 response to DELETE / carries a well-formed content-type header
**Feature:** Root Resource API · **Priority:** P2 · **Type:** data · **Severity:** Moderate

Resilience/header sanity: even on a rejected method the response must advertise a valid media type rather than an empty or malformed content-type.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 405; header "content-type" contains "/".

**Tags:** api, headers

### TC-075 — Confirm DELETE / with an Accept: application/json header still returns 405
**Feature:** Root Resource API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Content negotiation should not turn a disallowed method into a success; requesting JSON must not change the 405 outcome.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, content-negotiation

### TC-076 — Reject DELETE /99999999 for a non-existent id with a 404 or 405 (never 2xx)
**Feature:** Root Resource API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Unknown resource under the root: DELETE against an id that cannot exist must be a clean client error, never a success.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 404, 405. → Expected: The response status code is one of 404, 405.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 404, 405.

**Tags:** api, negative, not-found

### TC-077 — Reject DELETE /not-a-number for a malformed non-numeric id with a 4xx status
**Feature:** Root Resource API · **Priority:** P1 · **Type:** negative · **Severity:** Major

The id parameter is numeric; a non-numeric path segment must produce a clean client error, not a 2xx and not a 500.

**Precondition:** The API endpoint https://postman-echo.com/not-a-number is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/not-a-number. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, validation

### TC-078 — Confirm DELETE /?debug=true ignores an unknown query parameter and still returns 405
**Feature:** Root Resource API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

An unrecognised query parameter must be ignored: the method rejection takes precedence and the server must not error out.

**Precondition:** The API endpoint https://postman-echo.com/?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.

**Expected Result:** All 1 assertion pass — response status is 405.

**Tags:** api, query, robustness

### TC-079 — Confirm DELETE / with a trailing-slash path variant does not return a 500
**Feature:** Root Resource API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Path robustness: a trailing slash on the root must be handled gracefully (still a client-side rejection), never a server error.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, robustness, path

### TC-080 — Confirm DELETE / with an unexpected request body is still rejected with 405
**Feature:** Root Resource API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

A body on a DELETE is unusual; supplying one must not bypass the method restriction, and the server must not choke on it.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com carrying the JSON request body shown above. [data: {"unexpected":"payload"}] → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is one of 405, 400, 415. → Expected: The response status code is one of 405, 400, 415.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is one of 405, 400, 415.

**Tags:** api, robustness

### TC-081 — Confirm GET / on the same root URL succeeds (2xx), proving the 405 is DELETE-specific
**Feature:** Root Resource API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Contrast check against a documented sibling operation: the root accepts GET, so the 405 is bound to the DELETE method rather than the URL itself.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.

**Expected Result:** All 1 assertion pass — response is successful (2xx).

**Tags:** api, method, contrast

### TC-082 — Confirm DELETE / rejects an unauthenticated request the same way (405), since the API uses no auth
**Feature:** Root Resource API · **Priority:** P2 · **Type:** security · **Severity:** Moderate

This API defines no authorization; sending no credentials must not alter the outcome, which remains the method-based 405 rather than a 401/403.

**Precondition:** The API endpoint https://postman-echo.com is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 405. → Expected: The response status code is exactly 405.
3. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.

**Expected Result:** All 2 assertions pass — response status is 405; response is an error (non-2xx).

**Tags:** api, auth, no-auth

### TC-083 — Ensure DELETE / with a SQL-injection-shaped query value returns a clean 4xx without echoing the payload
**Feature:** Root Resource API · **Priority:** P2 · **Type:** negative · **Severity:** Major

Injection probe: a script/SQL-shaped query value must yield a clean client error and the raw injection string must not be reflected back in the response body.

**Precondition:** The API endpoint https://postman-echo.com/?q=1%27%20OR%20%271%27%3D%271 is reachable from the test environment.

**Steps:**

1. Send an HTTP DELETE request to https://postman-echo.com/?q=1%27%20OR%20%271%27%3D%271. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, security, injection

### TC-084 — Verify HEAD / returns 200 with a content-type header on the postman-echo root
**Feature:** Root API · **Priority:** P0 · **Type:** api · **Severity:** Critical

Confirms the documented HEAD request against the root resource responds successfully and advertises a content type, even though HEAD carries no response body.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that header "content-type" contains "/". → Expected: The "content-type" response header is present and contains "/".

**Expected Result:** All 2 assertions pass — response status is 200; header "content-type" contains "/".

**Tags:** api, smoke, head

### TC-085 — Confirm HEAD / responds within 5000ms on the happy path
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Guards the response-time budget for the root HEAD request so latency regressions are caught.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.
3. Assert that response time is under 5000 ms. → Expected: The endpoint responds in under 5000 ms.

**Expected Result:** All 2 assertions pass — response status is 200; response time is under 5000 ms.

**Tags:** api, performance, head

### TC-086 — Confirm HEAD / is a 2xx success and never returns a 5xx
**Feature:** Root API · **Priority:** P1 · **Type:** api · **Severity:** Major

Sanity resilience check that the root HEAD endpoint stays healthy and does not surface a server error.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is successful (2xx). → Expected: The response status code is in the 2xx success range.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is successful (2xx); response status is under 500.

**Tags:** api, resilience, head

### TC-087 — Return 4xx for HEAD /99999999 when the id cannot exist
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Requests a HEAD against an out-of-range numeric id under the root resource and expects a non-2xx (not-found) response.

**Precondition:** The API endpoint https://postman-echo.com/99999999 is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/99999999. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, head

### TC-088 — Return 4xx for HEAD /not-a-real-path when the path segment is non-numeric
**Feature:** Root API · **Priority:** P1 · **Type:** negative · **Severity:** Major

Sends a HEAD to a clearly invalid, non-numeric path under the root resource and expects a client error rather than success.

**Precondition:** The API endpoint https://postman-echo.com/not-a-real-path is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/not-a-real-path. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, head

### TC-089 — Confirm HEAD /?debug=true ignores an unknown query parameter and still returns 200
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Verifies an unrecognised query parameter is tolerated on the root HEAD request and does not trigger a 500.

**Precondition:** The API endpoint https://postman-echo.com/?debug=true is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/?debug=true. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, edge, head

### TC-090 — Confirm HEAD / with Accept: application/xml still returns a non-5xx response
**Feature:** Root API · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Content-negotiation probe: an unsupported Accept type must not crash the root HEAD endpoint.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, content-negotiation, head

### TC-091 — Confirm HEAD / tolerates an arbitrary custom request header and returns 200
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Resilience check that supplying an unrecognised custom header does not break the root HEAD request.

**Precondition:** The API endpoint https://postman-echo.com/ is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is 200. → Expected: The response status code is exactly 200.

**Expected Result:** All 1 assertion pass — response status is 200.

**Tags:** api, resilience, head

### TC-092 — Confirm HEAD /99999999?foo=bar keeps returning 4xx when a query is appended to an unknown id
**Feature:** Root API · **Priority:** P2 · **Type:** negative · **Severity:** Moderate

Verifies that appending a query string to an out-of-range id does not turn a not-found into a success.

**Precondition:** The API endpoint https://postman-echo.com/99999999?foo=bar is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/99999999?foo=bar. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response is an error (non-2xx). → Expected: The response status code is a non-2xx error, as this negative case expects.
3. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 2 assertions pass — response is an error (non-2xx); response status is under 500.

**Tags:** api, negative, head

### TC-093 — Confirm case-variant path HEAD /NoSuchResource does not return a 500
**Feature:** Root API · **Priority:** P3 · **Type:** edge · **Severity:** Minor

Path-variance resilience check ensuring an unknown mixed-case path yields a clean client error, never a server crash.

**Precondition:** The API endpoint https://postman-echo.com/NoSuchResource is reachable from the test environment.

**Steps:**

1. Send an HTTP HEAD request to https://postman-echo.com/NoSuchResource. → Expected: The endpoint accepts the request and returns an HTTP response — status code, headers and body — that the assertions below validate.
2. Assert that response status is under 500. → Expected: The response status code is below 500.

**Expected Result:** All 1 assertion pass — response status is under 500.

**Tags:** api, edge, head

### TC-094 — Verify root lifecycle error handling: create rejected (405) → read missing (404)
**Feature:** root flows · **Priority:** P1 · **Type:** e2e · **Severity:** Major

Attempts to create a root resource via POST to the echo base which is not allowed (405), then reads a non-existent root id and confirms it is not found (404). No id is minted by create, so the read exercises the documented missing-resource path.

**Precondition:** The 2 endpoints in this flow are reachable and the environment allows creating and deleting root records.

**Steps:**

1. Step 1 (create): send POST https://postman-echo.com. → Expected: The response status code is exactly 405.
2. Step 2 (read): send GET https://postman-echo.com/99999999. → Expected: The response status code is exactly 404.

**Expected Result:** All 2 assertions pass — create: response status is 405; read: response status is 404.

**Tags:** api, flow, regression
