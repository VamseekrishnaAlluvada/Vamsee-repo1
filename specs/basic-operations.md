# Test Plan — Basic Operations

_Generated by JBS IntelliQE._ This plan enumerates the automated scenarios in this
suite, grouped by module. Each numbered scenario maps to the correspondingly
numbered test in `tests/<module>/<module>.spec.ts`.

- **Total scenarios:** 6
- **Modules:** API

## API

### 1. Verify GET /v1/users returns 200 with a JSON body
**Feature:** Users API · **Priority:** P0 · **Type:** api

**Precondition:** The API endpoint https://api.example.com/v1/users?page=1&limit=10 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.example.com/v1/users?page=1&limit=10 → Expected: The API returns a response
2. Assert: response status is 200 → Expected: Assertion holds
3. Assert: header "content-type" contains "json" → Expected: Assertion holds
4. Assert: response time is under 5000 ms → Expected: Assertion holds

**Expected Result:** response status is 200; header "content-type" contains "json"; response time is under 5000 ms

### 2. Verify GET /v1/users response is 2xx and returns a well-formed body
**Feature:** Users API · **Priority:** P1 · **Type:** data

**Precondition:** The API endpoint https://api.example.com/v1/users?page=1&limit=10 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.example.com/v1/users?page=1&limit=10 → Expected: The API returns a response
2. Assert: response is successful (2xx) → Expected: Assertion holds
3. Assert: header "content-type" contains "application/json" → Expected: Assertion holds

**Expected Result:** response is successful (2xx); header "content-type" contains "application/json"

### 3. Reject GET /v1/users when the Authorization header is missing
**Feature:** Users API · **Priority:** P0 · **Type:** security

**Precondition:** The API endpoint https://api.example.com/v1/users?page=1&limit=10 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.example.com/v1/users?page=1&limit=10 → Expected: The API returns a response
2. Assert: response is an error (non-2xx) → Expected: Assertion holds
3. Assert: response status is one of 401, 403 → Expected: Assertion holds

**Expected Result:** response is an error (non-2xx); response status is one of 401, 403

### 4. Reject unsupported POST method on /v1/users collection
**Feature:** Users API · **Priority:** P2 · **Type:** negative

**Precondition:** The API endpoint https://api.example.com/v1/users?page=1&limit=10 is reachable from the test environment.

**Steps:**

1. Send an HTTP POST request to https://api.example.com/v1/users?page=1&limit=10 with the JSON request body → Expected: The API returns a response
2. Assert: response is an error (non-2xx) → Expected: Assertion holds
3. Assert: response status is under 500 → Expected: Assertion holds

**Expected Result:** response is an error (non-2xx); response status is under 500

### 5. Reject GET /v1/users with invalid pagination parameters
**Feature:** Users API · **Priority:** P2 · **Type:** edge

**Precondition:** The API endpoint https://api.example.com/v1/users?page=-1&limit=abc is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.example.com/v1/users?page=-1&limit=abc → Expected: The API returns a response
2. Assert: response is an error (non-2xx) → Expected: Assertion holds
3. Assert: response status is under 500 → Expected: Assertion holds

**Expected Result:** response is an error (non-2xx); response status is under 500

### 6. Return 404 for an unknown users sub-resource path
**Feature:** Users API · **Priority:** P3 · **Type:** negative

**Precondition:** The API endpoint https://api.example.com/v1/users/does-not-exist-000 is reachable from the test environment.

**Steps:**

1. Send an HTTP GET request to https://api.example.com/v1/users/does-not-exist-000 → Expected: The API returns a response
2. Assert: response is an error (non-2xx) → Expected: Assertion holds
3. Assert: response status is 404 → Expected: Assertion holds

**Expected Result:** response is an error (non-2xx); response status is 404
